# AGENTS.md

在开始任何分析、设计、编码、修改、重构、联调、测试、文档修订之前，必须先完整阅读本文件，并严格遵守以下规则。

## 1. 任务定位

你不是在“自由设计一个新产品”，而是在现有项目基础上，基于历史代码、历史文档和生产环境，对一个模型训练平台进行高保真还原、补全、修正和工程化落地。

你的最终目标是：

- 还原并补齐标准产品能力
- 修正与生产环境不一致的实现
- 同时覆盖前端和后端，而不是只做前端页面
- 最终交付为一个可运行、可构建、可部署的网站系统
- 在不改变业务本质的前提下，对 UI 和交互进行专业化升级

同时必须支持以下后续场景：

- 用户可能在生产环境基线之上，主动提出新增功能或修改功能
- 对于这类“用户明确提出的新需求”，不能机械受限于当前生产环境
- 你需要在生产环境基线之上完成实现，并同步修正或补充 PRD / RPD

## 2. 产品范围

本项目标准产品范围以生产环境和用户明确需求为准。

其中：

- 数据管理、模型训练、模型评估是主线核心能力
- 模型服务、系统管理等标准平台模块同样属于必要范围
- 机器学习等模块是否继续增强，应结合生产环境实际状态和用户当轮要求判断

注意：
你开发的是“标准产品能力”，不是面向特定业务场景临时定制的一组功能集合。
只有用户已经明确排除的模块，才不需要开发或继续增强。

## 3. 唯一真相源与信息优先级

### 3.1 默认基线规则

当信息发生冲突时，默认严格按照以下优先级判断：

1. 生产环境
2. 当前代码库
3. RPD / PRD / 历史文档 / 原型说明

硬性规则：

- 生产环境是默认的功能真相源
- RPD / PRD 并不完全可靠，可能存在偏差、遗漏、过时内容或错误表达
- 如果 RPD / PRD 与生产环境冲突，一律以生产环境为准
- 如果历史代码与生产环境冲突，一律以生产环境为准
- 如果文档、代码、原型都无法说明，但生产环境已有明确表现，则以生产环境为准

### 3.2 用户主动新增或修改需求时的规则

如果用户明确提出：

- 新增功能
- 修改现有功能
- 改写某段流程
- 调整信息架构
- 补充生产环境当前没有、但后续要新增的标准能力

则在“用户明确指定的影响范围内”，优先级改为：

1. 用户当前明确需求
2. 生产环境
3. 当前代码库
4. RPD / PRD / 历史文档

这意味着：

- 生产环境仍然是默认基线
- 但用户明确提出的新需求，是对基线的“受控演进”
- 你必须把这类变更视为正式需求，而不是“偏离生产环境的异常”
- 你必须同步更新 PRD / RPD 理解，必要时直接补充或修正文档内容
- 你必须明确区分“生产环境原有能力”和“用户新增/修改后的目标能力”

## 4. 必须遵守的工作顺序

禁止跳步。必须按以下顺序推进：

1. 阅读代码库关键文件，理解现有系统结构
2. 梳理当前前端、后端、接口、路由、数据结构、页面和模块状态
3. 识别并标记受保护模块
4. 识别并标记不需要开发的非标准功能
5. 完整阅读并系统拆解生产环境
6. 基于生产环境反向修正产品理解
7. 修正对 RPD / PRD 的理解与使用方式
8. 如果用户提出新增或修改需求，明确记录其相对生产环境的变化点
9. 输出差异清单、PRD 修正点与开发优先级
10. 按优先级逐模块开发和修正
11. 每完成一个阶段进行自检和回归验证
12. 最终确认系统具备部署条件

在完成“生产环境完整拆解”之前，不得正式进入开发。

## 5. 生产环境拆解要求

你不能只浏览几个页面后就开始编码。你必须对生产环境进行系统性拆解，至少覆盖以下内容：

### 5.1 模块层
- 导航结构
- 一级模块
- 二级模块
- 页面层级关系
- 页面跳转关系
- 入口与出口路径
- 核心用户主流程

### 5.2 页面层
- 列表页
- 创建页
- 编辑页
- 详情页
- 配置页
- 任务页
- 结果页
- 设置页

### 5.3 页面元素层
- 表格列
- 表单字段
- 字段类型
- 字段校验
- 默认值
- 提示文案
- 按钮文案与位置
- 操作入口
- 筛选项
- 搜索项
- 分页行为
- Tab 结构
- 弹窗 / 抽屉 / Step 流程

### 5.4 状态与行为层
- 空态
- 加载态
- 错误态
- 禁用态
- 成功态
- 任务状态
- 状态流转
- 按钮显隐逻辑
- 交互反馈
- 提交后行为
- 返回路径

### 5.5 数据与后端行为层
- 前后端交互方式
- 数据对象关系
- 核心实体
- 列表与详情的数据映射关系
- 任务创建、执行、查看、回溯链路
- 可能存在的权限和可见性差异
- 结果数据组织方式

必须做到“整体阅读并拆解”，而不是局部观察。

## 6. PRD / RPD 使用与同步原则

RPD / PRD 只能作为辅助参考，不是直接执行依据。

你必须先完成生产环境拆解，再去判断：

- 哪些 RPD / PRD 内容是准确的
- 哪些内容过时了
- 哪些内容描述不完整
- 哪些地方与生产环境有偏差
- 哪些功能定义需要被修正

也就是说，默认顺序是：

先看生产环境 -> 再修正文档理解 -> 再输出修正后的产品认知 -> 再进入开发

### 6.1 当用户主动增加或修改需求时

如果用户明确提出新需求，必须同步做两件事：

1. 修改实现
2. 修改或补充 PRD / RPD

同步要求：

- 先说明该需求相对生产环境的变化点
- 再说明它会如何影响页面、流程、字段、接口和数据结构
- 再同步产出 PRD / RPD 修正理解
- 开发完成后，再确认实现与修正后的文档是否一致

禁止：

- 直接按 RPD / PRD 编码
- 把文档中的错误定义带入系统实现
- 在未完成生产环境核对前就反向修改业务逻辑
- 用户已经明确提出变更后，却只改代码不改文档理解

## 7. 页面内嵌设计文档规则

默认把“页面内可编辑需求说明与页面功能融合”视为本项目的标准协作方式。

目标形态：

- 页面右下角有一个浮动按钮
- 点击后展开需求说明面板
- 面板应尽量采用“推挤主内容区”的方式，而不是粗暴遮挡主界面
- 打开页面时，用户既能查看交互效果，也能直接查看当前页面的设计说明
- 面板中的运行态需求说明应支持直接编辑
- 运行态需求说明默认保存在当前页面本地，不与仓库中文档文件自动同步

执行规则：

- 只要修改某个页面功能、字段、状态、交互、布局或流程，就必须同步检查并更新该页面的内嵌设计文档
- 只要新增页面，就必须为该页面建立对应的内嵌设计文档内容
- 需求说明面板不是静态装饰，而是页面的一部分
- 运行态需求说明应服务于产品经理直接记录和补充需求
- 仓库中的文档模板仅用于 AI 工作流参考，不强绑定到运行态面板内容

运行态需求说明面板至少应支持：

- 直接编辑需求内容
- 新增多条需求说明
- 展示每条需求的创建时间
- 保留系统侧变更记录

## 8. 受保护模块

以下模块是受保护模块：

- 模型训练 - 大模型训练

该模块已经满足要求。除非用户后续明确提出，否则禁止修改此模块的任何内容，包括但不限于：

- 页面结构
- UI 样式
- 交互逻辑
- 路由
- 前端组件
- 接口绑定
- 后端逻辑
- 数据结构
- 文案
- 状态行为

默认将其视为只读区域。

如果其他修改可能影响该模块，必须优先绕开，而不是顺手重构。

## 9. 明确不开发的功能

以下功能不是标准产品能力，不需要开发，也不要继续增强：

- 业务测试数据集
- 业务推理结果集
- 文件管理
- 业务效果评估
- API 服务

如果这些内容已经存在于当前项目中：

- 不要继续扩展
- 不要作为标准产品能力保留
- 不要围绕它们做额外设计
- 如有必要，只能做隔离、隐藏、降级或弱化处理
- 不能让这些功能影响标准产品信息架构和主流程

如果用户后续明确要求恢复或新增上述能力，必须先与当前标准范围冲突做显式说明，再执行。

## 10. 功能复刻与受控演进原则

对需要开发、补齐或修正的模块，必须遵循以下原则：

- 默认以生产环境为基线进行高保真复刻
- 页面关系尽可能与生产环境一致
- 核心字段尽可能与生产环境一致
- 关键流程尽可能与生产环境一致
- 状态与反馈尽可能与生产环境一致
- 前后端行为尽可能与生产环境一致
- 不能只做表面页面，还要支撑真实业务链路

如果用户主动提出新增或修改需求：

- 以生产环境为基线理解现状
- 以用户新需求为目标理解未来态
- 明确标记“基线能力”和“新增/变更能力”
- 同步更新 PRD / RPD 理解
- 再进行实现

禁止以下行为：

- 擅自增加功能
- 擅自删除核心能力
- 擅自改变业务语义
- 擅自改变核心字段定义
- 擅自缩短或简化关键流程
- 用静态页面冒充已完成能力
- 用 mock 替代必须存在的真实逻辑而不标记

## 11. UI 与交互优化原则

生产环境是功能标准，不是视觉标准。

你可以优化：

- UI 风格
- 页面布局
- 视觉层级
- 组件表达
- 操作动线
- 交互反馈
- 表单可读性
- 列表信息组织
- 状态反馈体验

但必须同时满足：

- 功能不变，除非用户明确要求变更
- 业务语义不变，除非用户明确要求变更
- 默认信息架构不变，除非用户明确要求变更
- 不降低专业性
- 不牺牲复杂业务表达能力
- 不为了“更好看”而丢失真实业务细节

视觉与交互方向应符合：

- 专业 B 端产品风格
- 清晰的信息层级
- 克制而统一的设计语言
- 更强的可读性和可发现性
- 更顺畅的主流程体验
- 更完整的空态、加载态、错误态、禁用态设计

## 12. UI 原型与截图复刻规则

如果用户提供了：

- 页面截图
- 原型图
- 设计稿
- 参考界面
- “照这个做原型”
- “参考这个图还原页面”

则优先使用已安装的 `protodesign` skill 或等效截图还原工作流进行分析和实现。

特别规则：

- 如果截图中体现的是“页面 + 内嵌需求说明侧板”的组合形态，需要把它视为页面结构的一部分，而不是临时辅助层
- 要同时分析主内容区、浮动按钮、侧板展开方式、推挤关系、标签页、文档内容区和关闭交互
- 如果用户没有特别说明，默认优先采用“展开后推挤主内容区”的方式，而不是遮挡

注意：

- 视觉参考只用于 UI 和交互表达
- 功能、字段、流程的判断，仍应优先结合生产环境和用户需求
- 不要让截图参考覆盖业务真相源规则

## 13. 前后端一体化要求

本项目不是纯前端项目。开发时必须同时考虑：

- 前端页面与交互
- 后端接口与业务逻辑
- 数据结构
- 状态流转
- 列表与详情数据组织
- 提交、保存、编辑、查询、删除、执行等真实动作
- 联调关系
- 构建与部署可行性

如果当前仓库后端能力缺失或不完整，需要基于生产环境表现与现有结构合理补齐。

目标是：

- 不是原型
- 不是演示稿
- 不是只有前端壳子
- 而是一个可部署的网站系统

## 14. 工程约束

开发时必须遵守以下工程原则：

- 尽量复用当前技术栈和项目结构
- 不随意引入重型依赖
- 不做与目标无关的大规模重构
- 优先复用已有组件、服务和工具方法
- 代码命名必须清晰、语义化
- 组件拆分合理，不要过度抽象
- 保持前后端结构可维护
- 保持代码可读、可持续迭代
- 对临时 mock、占位逻辑、待替换逻辑进行明确标记
- 不要为了追求“架构漂亮”而损害交付效率和业务还原度

## 15. 安全与敏感信息规则

如果用户提供了测试环境或生产环境地址、账号、密码，只能用于运行时核对和验证，不得：

- 写入代码
- 写入配置文件
- 写入 README / AGENTS / 文档模板
- 写入提交信息
- 输出到日志
- 固化到脚本中

敏感信息只能临时使用，不能沉淀到仓库。

## 16. 每次开工时的默认动作

每次开始新一轮工作时，默认先做以下事情：

1. 阅读本文件
2. 阅读项目关键代码和相关文档
3. 确认受保护模块
4. 确认非标准功能边界
5. 核对是否已有生产环境拆解结果
6. 如果拆解不完整，优先补齐拆解
7. 如果用户提出新需求，先明确其相对生产环境的变化点
8. 如果本轮涉及 UI 页面，明确该页面的运行态需求说明面板是否需要调整
9. 在差异清单基础上判断本轮要处理的模块
10. 再进入开发

## 17. 每轮输出要求

每次输出都尽量结构化，至少包含：

1. 当前对系统的理解
2. 当前处理的模块
3. 生产环境拆解结论
4. 当前代码与生产环境的差异
5. 用户新增/修改需求与生产环境的差异（如果存在）
6. 对 RPD / PRD 的修正理解
7. 页面内需求说明面板需要调整的内容（如果存在）
8. 本次实施方案
9. 实际修改内容
10. 自检结果
11. 风险点
12. 下一步建议

不要只讲笼统计划，不要空泛表述。

## 18. 必须产出的中间文档或结果

在完整开发前，应逐步形成以下内容：

### 18.1 生产环境拆解结果
至少应包括：
- 模块地图
- 页面清单
- 主流程图
- 核心实体与关系
- 页面功能说明
- 关键状态说明

### 18.2 差异清单
按模块列出：
- 当前已有内容
- 生产环境对应内容
- 缺失点
- 偏差点
- 用户新增/修改点
- 修正建议
- 风险点
- 优先级

### 18.3 PRD / RPD 修正理解
当发现文档偏差或用户提出新需求时，应明确说明：
- 原文档的问题
- 生产环境的真实表现
- 用户新增/修改后的目标表现
- 修正后的理解
- 对开发实现的影响

### 18.4 页面内需求说明面板
对于每个进入开发的页面，应明确：
- 当前页面是否已接入可编辑需求说明面板
- 需求说明是否保存在页面本地
- 变更记录是否保留
- 是否避免与仓库文件产生自动强同步

## 19. 开发优先级原则

默认优先级排序规则：

1. 主流程必经模块优先
2. 影响整体闭环的模块优先
3. 与生产环境差异最大的关键模块优先
4. 用户明确优先要求的新增或修改模块优先
5. 前后端联动强的模块优先
6. 页面内嵌设计文档能力的底层壳层建设应尽早纳入
7. 边缘页面和装饰性优化靠后

不要先做无关紧要的小页面，也不要优先做纯视觉修饰。

## 20. 自检与验收要求

每完成一个模块或阶段，必须至少检查：

- 页面是否可访问
- 路由是否正确
- 功能入口是否完整
- 列表、创建、编辑、详情是否闭环
- 前后端逻辑是否打通
- 关键状态是否覆盖
- 与生产环境是否一致
- 对于用户新增/修改需求，是否与新目标一致
- 页面内需求说明面板是否已按需求调整
- 设计文档面板交互是否顺滑
- 设计文档展开时是否按预期推挤或组织主内容区
- 是否误改受保护模块
- 是否误增强非标准功能
- 是否仍满足部署目标
- PRD / RPD 理解是否已同步更新

## 21. 成功标准

任务只有在以下条件同时满足时才算完成：

- 标准产品核心能力与生产环境高度一致，或在用户明确要求下完成受控演进
- 缺失模块已经补齐
- 偏差模块已经修正
- 用户新增或修改的需求已同步落地
- 前端和后端都已覆盖
- 项目具备可运行、可构建、可部署能力
- 模型训练 - 大模型训练模块未被误改
- 非标准功能未被继续开发，除非用户明确要求
- UI 与交互相较生产环境有明显提升
- PRD / RPD 理解已随变更同步修正
- 页面内可编辑需求说明能力已建立并可稳定使用
- 代码结构清晰，后续可维护、可扩展

## 22. 默认执行口令

除非用户明确要求仅讨论方案，否则默认按以下方式执行：

- 先读
- 再拆
- 再比
- 再修正文档理解
- 如果有新增需求，再明确未来态
- 如果涉及页面，再同步考虑内嵌设计文档
- 再计划
- 再开发
- 再自检
- 再汇报

禁止在没有完成上下文理解前直接进入随意编码。
