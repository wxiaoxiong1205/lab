# AGENTS.md

在开始任何分析、设计、编码、修改、重构、联调、测试、文档修订之前，必须先完整阅读本文件，并严格遵守以下规则。

## 1. 第二阶段定位

第一阶段“基于生产环境的高保真复刻与标准能力补齐”已经结束。

从现在开始，本项目进入第二阶段：

- 以**当前仓库已验收结果**为新的实现基线
- 以后续**用户每一轮明确需求、截图、草图、原型和交互说明**为主要驱动
- 持续进行信息架构调整、交互优化、功能扩展和受控重构
- 目标仍然是交付一个**可运行、可构建、可部署**的网站系统

这意味着：

- 你不再默认把“生产环境复刻”当作每轮前置目标
- 你不再默认把第一阶段的限制和优先级当作最高约束
- 你要围绕用户后续的每一轮需求，做稳定、连续、可追踪的迭代开发

## 2. 第二阶段唯一真相源与优先级

从第二阶段开始，默认优先级调整为：

1. 用户当前明确需求
2. 用户提供的截图、原型、参考图、交互说明
3. 当前仓库中已经验收通过的实现
4. 第二阶段 AI 协作指南与模板
5. 第一阶段历史文档、生产环境拆解文档、PRD、探索报告

特别规则：

- 第一阶段文档只作为**历史背景材料**，不再作为默认执行基线
- 如果用户没有要求参考生产环境，则不要主动回退到第一阶段“按生产环境对齐”的工作模式
- 如果用户明确要求“再去看生产环境”或“和生产环境对比”，才重新启用生产环境作为参考输入
- 如果当前代码和用户新需求冲突，以用户新需求为准

## 3. 第一阶段历史材料的使用方式

以下材料属于**历史归档**，只在需要回溯背景时使用，不作为后续默认开发依据：

- `docs/ai/production-review-round1.md`
- `docs/ai/module-delivery-data-service-round*.md`
- `docs/ai/next-phase-module-plan.md`
- `DeepexiLab平台探索报告.md`
- `DeepexiLab-Platform-Exploration.md`
- `PRD.md`
- `screenshots/` 中第一阶段用于生产环境拆解的资料

默认原则：

- 不要被这些文档中的“必须先拆生产环境”“受保护模块”“阶段性排除项”等第一阶段策略机械约束
- 如果某条第一阶段结论与当前用户需求冲突，以当前用户需求为准
- 如果需要引用历史材料，必须明确说明“这是历史阶段结论，不是当前默认基线”

## 4. 第二阶段默认工作顺序

第二阶段禁止沿用“先全站拆生产环境再开发”的固定流程。

每次新一轮任务，默认按以下顺序执行：

1. 阅读本文件
2. 阅读与本轮需求直接相关的代码、页面、状态、路由和文档
3. 判断本轮需求属于：
   - 页面改版
   - 信息架构调整
   - 新功能补充
   - 交互优化
   - 权限/状态/数据结构调整
   - 前后端联动
4. 如果用户提供了截图、原型或草图，优先按截图拆解 UI 与交互
5. 输出本轮理解、影响范围、实施方案和风险点
6. 再进入开发
7. 同步更新页面内嵌设计文档
8. 进行构建、自检和回归验证

只有当用户明确要求对比生产环境时，才插入“生产环境核对”步骤。

## 5. 第二阶段产品范围

第二阶段不再沿用第一阶段“固定标准范围 + 固定排除项”的默认模式。

从现在开始：

- 默认开发范围以**用户当前明确要求**为准
- 用户要求修改哪个模块，就处理哪个模块
- 用户要求恢复、重做、删除、弱化、重组某个模块，都视为正式需求
- 不再默认认为某些模块永久受保护或永久禁止开发

但仍需遵守：

- 不要擅自扩大需求范围
- 不要擅自删除未被点名的核心能力
- 不要为了重构而重构
- 不要破坏当前已通过验收的实现

## 6. 截图 / 原型 / 参考图规则

如果用户提供：

- 页面截图
- 原型图
- 竞品参考
- “按这个做”
- “改成这种布局”
- “照这个还原”

则必须优先按截图进行拆解与实现。

执行要求：

- 视觉与交互以截图为准
- 功能语义以用户文字说明为准
- 如果截图与用户文字冲突，以用户文字说明为准
- 如果截图体现的是壳层结构、顶栏、侧栏、卡片、面板、文档侧板等整体布局，也要视为正式需求，而不是“仅做样式参考”

如果涉及截图驱动的页面还原，优先使用已安装的 `protodesign` skill 或等效工作流。

## 7. 页面内嵌设计文档规则

第二阶段继续保留并强化“页面功能 + 页面内可编辑需求说明”的协作方式。

硬性规则：

- 只要修改某个页面的功能、字段、状态、布局、信息架构、交互或流程，就必须同步检查该页面的右侧需求文档侧板
- 只要新增页面，就必须为该页面补对应的设计文档内容
- 页面内运行态需求说明默认保存在本地，不自动同步仓库文档

第二阶段要特别补充：

- 如果页面发生了信息架构变化，设计文档必须明确记录“旧结构 -> 新结构”
- 如果页面是基于截图做的新样式或新布局，也要把截图驱动的关键变化写入侧板
- 如果需求已不再参考生产环境，文档里要写明“当前页以用户迭代需求为基线”

## 8. 文档与模板使用方式

第二阶段长期文件如下：

- `AGENTS.md`
- `docs/ai/phase2-operating-guide.md`
- `docs/ai/production-review-template.md`
- `docs/ai/module-delivery-template.md`
- `docs/ai/page-design-doc-template.md`

其中：

- `production-review-template.md`
  在第二阶段中不再表示“生产环境拆解模板”，而是表示**需求拆解 / 迭代规划模板**
- `module-delivery-template.md`
  用于每一轮功能实现与交付记录
- `page-design-doc-template.md`
  用于页面内嵌设计文档内容整理参考

## 9. 前后端一体化要求

第二阶段仍然不是“纯前端原型阶段”。

你必须同时考虑：

- 前端页面与交互
- 数据结构
- 状态流转
- 接口边界
- 本地 mock / 本地后端 / 真实后端的演进路径
- 构建与部署可行性

要求：

- 如果当前需求只需前端改动，可以先做前端
- 但如果需求涉及状态、权限、项目上下文、任务流转、实体关系、提交保存等，就必须同步考虑数据层和后端边界
- 不能用纯静态页面伪装成已经完成的业务能力

## 10. 工程约束

第二阶段继续遵守以下工程原则：

- 尽量复用当前技术栈和项目结构
- 不随意引入重型依赖
- 不做与目标无关的大规模重构
- 优先复用已有组件、服务、状态层和工具方法
- 保持代码可读、语义清晰、便于后续持续迭代
- 对临时 mock、占位逻辑、待接后端逻辑明确标记

新增约束：

- 壳层改造时，优先抽象“导航域”“上下文域”“页面域”，避免后续每次改顶栏都牵一发而动全身
- 如果需求是信息架构调整，优先先改路由、壳层、导航，再改页面细节

## 11. 安全与敏感信息

如果用户提供测试环境、生产环境、账号、密码、Token 或内部链接：

- 只能用于临时核对
- 不得写入代码、配置、日志、文档或提交信息
- 不得固化进脚本

## 12. 每次开工时的默认动作

每次开始新一轮工作时，默认先做：

1. 阅读本文件
2. 阅读与本轮需求直接相关的页面、组件、服务、路由、文档
3. 判断本轮需求涉及哪些页面内嵌设计文档
4. 判断是否需要参考截图 / 原型
5. 输出本轮理解、影响范围和实施方案
6. 再进入开发

不再默认做：

- 全站生产环境拆解
- 全模块差异清单
- 第一阶段式模块盘点

除非用户明确要求。

## 13. 每轮输出要求

第二阶段每次输出尽量包含：

1. 当前对需求的理解
2. 当前处理的模块 / 页面
3. 当前代码与目标效果的差异
4. 截图 / 用户说明中的关键要求
5. 本次实施方案
6. 是否涉及页面内嵌设计文档调整
7. 实际修改内容
8. 自检结果
9. 风险点
10. 下一步建议

如果本轮不开发，只讨论方案，也要给出：

- 目标结构
- 影响范围
- 分阶段实施建议

## 14. 第二阶段成功标准

第二阶段的完成标准不再是“生产环境高保真复刻完成”，而是：

- 当前仓库持续成为用户已确认需求的最新实现
- 每轮需求都能在当前代码基线上稳定迭代
- 页面、路由、权限、状态、数据结构与用户当前目标一致
- 页面右侧内嵌设计文档持续同步
- 项目始终可运行、可构建、可部署
- 历史阶段材料不再干扰当前开发判断

## 15. 默认执行口令

第二阶段默认按以下方式执行：

- 先读当前需求
- 再读相关代码
- 再拆截图和交互
- 再定影响范围
- 再做方案
- 再开发
- 再同步页面内设计文档
- 再构建和自检
- 再汇报

禁止再按第一阶段的固定方式机械执行：

- 先全站拆生产环境
- 先做生产环境差异清单
- 先判断是否偏离生产环境

除非用户明确要求这么做。
