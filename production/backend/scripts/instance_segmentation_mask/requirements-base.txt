# -----------------------------------------------------------------------------
# 基础依赖（启动 ML 后端 / gunicorn 前安装）
#
# 为何不用 git+https 安装 label-studio-ml？
#   从 GitHub 安装时会执行 git clone，且上游 setup 可能再拉取 label-studio-sdk 的
#   git 依赖，在国内/内网环境容易长时间卡住或失败。
#
# 推荐做法：
#   1) 本目录在官方仓库内的路径为：.../label_studio_ml/examples/torch_instance_segmentation_mask_demo
#   2) 下面使用「可编辑安装」指向仓库根目录（../../../），只读本地源码，不访问 Git。
# -----------------------------------------------------------------------------
gunicorn==23.0.0

# 从当前示例目录向上三级即 label-studio-ml-backend 仓库根目录（含 setup.py）
-e ../../../
