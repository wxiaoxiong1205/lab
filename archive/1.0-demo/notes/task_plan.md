# 数据权限与项目成员权限升级计划

## Goal

在现有菜单权限、操作权限、项目上下文基础上，新增角色级数据权限，并让项目成员通过角色获得项目内操作能力和数据范围。租户管理员隐藏但默认拥有所有权限；其他角色按“个人数据/任务”和“全部数据”决定能否查看详情、编辑、删除、启动、下载等操作。

## Source Of Truth

- 用户 2026-05-22 已确认的 8 点权限口径。
- 当前仓库第二阶段已验收实现。
- 不默认参考生产环境。

## Phases

| Phase | Status | Scope |
| --- | --- | --- |
| 1 | complete | 梳理权限 store、权限配置页、项目管理页和资源操作入口 |
| 2 | complete | 升级 `permissionStore` 数据结构：动态角色、数据权限、租户管理员隐藏、项目成员角色 |
| 3 | complete | 改造权限配置页：操作权限 tab + 数据权限 tab + 自定义角色创建/编辑 |
| 4 | complete | 改造项目管理页：移除项目管理员字段，成员添加时选择该用户可用角色，成员表展示角色 |
| 5 | complete | 接入统一数据权限 helper 到高频资源操作：数据服务、机器学习、Notebook/模型/服务等核心入口 |
| 6 | complete | 同步页面内嵌设计文档、构建和必要验证 |
| 7 | complete | 二次扫尾：补直达路由、行内编辑、新建 creator 写入等漏网权限问题 |
| 8 | complete | 三次扫尾：脚本扫描行内编辑入口，补训练/测试/推理结果/Notebook 余量权限兜底并重新构建 |

## Decisions

- 列表允许看到和搜索到其他人的数据。
- 没有“全部数据”权限时，详情、编辑、删除、启动、下载等操作需要拦截并提示“权限不足”。
- 个人数据/任务按创建人账号判断；需要对历史 mock 创建人做账号映射，避免 `admin/lab1/deepexilab` 和真实账号不一致导致演示混乱。
- 系统管理中凡涉及人创建的资源也要走数据权限；纯平台配置仍以操作权限为主。
- 租户管理员不在权限配置和项目成员添加里展示，但登录态拥有所有权限。
- 内置三个可见角色不允许编辑；自定义角色允许创建和编辑。
- 项目成员角色只能从该用户在控制台已拥有的角色中选择。

## Risks

- 当前仓库已有未提交改动，且涉及 `permissionCatalog.ts`、`permissionStore.ts`、壳层和项目空间；必须增量修改，不回退。
- 资源模块较多，完整接入所有操作入口可能较大。本轮优先做统一 helper 和核心页面，避免散落判断。
- 现有很多页面仍是本地 mock，权限持久化仍在 localStorage，不代表真实后端鉴权。
- 本轮二次扫尾已覆盖当前静态扫描发现的 creator 资源主路径，但后续新增 mock 页面仍必须优先复用统一数据权限 helper，不能重新散落创建人判断。
- 行内编辑属于高风险绕过点，后续涉及名称/描述可编辑时必须先确认 `TaskMetadataEditor` 是否传入 `disabled` 且 `onSave` 内部有二次权限校验。

## Errors Encountered

| Error | Attempt | Resolution |
| --- | --- | --- |
| Browser local URL initially blocked / 5202 not running | 1 | 启动 Vite dev server 后验证权限配置与项目管理页面，验证后停止服务 |
