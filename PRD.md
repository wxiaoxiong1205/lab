# DeepexiLab 平台 PRD

> **文档版本**：V1.14
> **编写日期**：2026-04-04
> **平台名称**：DeepexiLab平台
> **访问地址**：https://deepexilab-test.deepexi.com

> **开发状态**：V1.4 基线版本开发已完成；V1.14 已补充权限配置需求，以下为功能范围：
> - 首页：任务卡片查看详情 Modal（查看详情 →）、卡片点击跳转、手风琴式详情展示
> - 数据服务：训练数据管理（行展开版本列表 + 详情 Modal）、测试数据管理（详情 Modal）、推理结果集（详情 Modal）
> - 数据处理：数据标注（详情 Modal + 开始标注按钮）、数据清洗（详情 Modal）
> - 模型训练：在线 Notebook（Tab 切换 + 创建表单 + 详情 Modal）、模型管理（创建表单 + 详情 Modal）
> - 模型评估：效果评估（详情 Modal）、评估指标（详情 Modal）
> - 模型服务：模型部署（详情 Modal + 启动/停止按钮联动）
> - 机器学习：数据管理（详情 Modal）、机器学习标注（详情 Modal + 开始标注按钮）、模型管理（新增）、模型部署（新增）、在线 Notebook（新增）
> - 系统管理：项目管理（详情 Modal 含成员管理和 SSH 配置标签页）、权限配置（操作权限 + 可操作资源范围）、集群管理（详情 Modal + 测试连接）、存储配置（详情 Modal + 测试连接）、镜像管理（详情 Modal 含镜像列表）、基础模型管理（详情 Modal）、系统配置（详情 Modal）、平台管理员（详情 Modal）
> - 通用组件：SharedListPage 组件增强（支持行展开 expandRow）

> **V1.14 补充范围**：系统管理-权限配置，补充操作权限、可操作资源范围、外部角色同步、项目成员角色分配和资源操作拦截规则。

---

## 一、产品概述

### 1.1 产品定位

DeepexiLab 是企业级 AI 开发和训练平台，提供从数据准备、数据处理、模型训练、模型评估到模型部署的全生命周期管理能力。平台基于分布式 GPU 集群，支持大语言模型（LLM）、视觉语言模型（VLM）、语音模型（ASR）、重排序模型（Embedding）及传统机器学习的统一开发体验，帮助企业快速构建和部署专属 AI 应用。

### 1.2 核心价值

- **全流程覆盖**：覆盖数据管理、数据处理、模型训练、模型评估、模型服务全链路
- **多场景支持**：支持文本生成、图像理解、语音识别、机器学习等多种训练与推理场景
- **多训练方法**：支持 SFT（监督微调）、DPO（直接偏好优化）、RM（奖励模型）、PPO（近端策略优化）四种训练方法
- **灵活微调策略**：大模型训练支持全参数微调和 LoRA 微调两种模式
- **强化学习支持**：支持从基础模型微调→奖励模型训练→PPO 强化学习优化的完整 RLHF 链路
- **模型复用**：支持从已训练模型继续微调，以及将训练模型作为强化学习基础
- **定时任务**：所有任务类型均支持定时启动配置
- **企业级管理**：支持多项目管理、Kubernetes 集群管理、存储配置、镜像仓库管理
- **开放集成**：提供标准 API 服务，支持第三方系统快速接入

### 1.3 技术基础

- 基于 **SFT / DPO / RM / PPO** 四种训练方法
- 支持多 GPU 分布式训练（A100 / V100 / T4 / H100）
- 集成 **bf16 精度训练**、梯度检查点、梯度裁剪、RoPE 缩放等优化技术
- 支持 Kubernetes 容器编排和 TOS / OSS / S3 / NFS / Ceph 等分布式存储
- 支持 vLLM / DGI Server 等推理引擎

---

## 二、功能架构

### 2.1 功能模块总览

```
DeepexiLab 平台
├── 首页（平台概览与快速入口）
├── 数据服务
│   ├── 训练数据管理
│   ├── 测试数据管理
│   └── 推理结果集
├── 数据处理
│   ├── 数据标注
│   └── 数据清洗
├── 模型训练
│   ├── 在线Notebook
│   ├── 大模型训练
│   └── 模型管理
├── 模型评估
│   ├── 效果评估
│   └── 评估指标
├── 模型服务
│   ├── 模型部署
│   └── 在线推理服务
├── 机器学习
│   ├── 数据管理
│   ├── 机器学习标注
│   ├── 模型管理
│   ├── 模型部署
│   └── 在线Notebook
├── 系统管理
│   ├── 项目管理
│   ├── 权限配置
│   ├── 集群管理
│   ├── 存储配置
│   ├── 镜像管理
│   ├── 基础模型管理
│   └── 系统配置
└── 平台管理员
```

---

## 三、首页

### 3.1 功能说明

首页为用户提供平台概览、快捷入口和最新任务进度展示，帮助用户快速了解平台运行状态并直达目标功能。

### 3.2 首页模块

- **平台欢迎区**：显示"欢迎使用 DeepexiLab"
- **统计卡片**：展示训练任务数、评估任务数、已完成任务数、GPU 使用率等核心指标
- **快捷入口卡片**：大数据训练、数据管理、效果评估等主要模块的快速入口
- **最新任务进度**：展示所有通过创建产生的任务，包括：
  - 大模型训练任务
  - 效果评估任务
  推理结果集任务
  - 数据标注任务
  - 数据清洗任务
  - 任务卡片显示：任务名称、任务类型标签（训练/评估/推理/标注/清洗）、运行状态、进度条、创建时间

### 3.3 首页任务卡片交互

#### 3.3.1 任务卡片点击行为

每个任务卡片同时支持两种交互方式：

| 交互区域 | 触发方式 | 行为说明 |
|----------|----------|----------|
| 卡片主体 | 点击卡片任意空白区域 | 跳转到对应模块页面 |
| "查看详情 →" 链接 | 点击卡片右下角的链接文字 | 打开任务详情 Modal |

#### 3.3.2 任务详情 Modal

点击"查看详情 →"后，弹出任务详情 Modal，Modal 包含以下内容：

**基本信息区域**：

| 字段 | 说明 |
|------|------|
| 任务名称 | 标注任务名称 |
| 任务类型 | 类型标签（训练/评估/推理/标注/清洗），带颜色 |
| 状态 | 状态标签（运行中/已完成/待执行），带颜色 |
| 创建时间 | 任务创建时间（YYYY/MM/DD HH:mm:ss） |

**进度信息区域**：

| 字段 | 说明 |
|------|------|
| 执行进度 | 进度条展示当前任务执行百分比 |
| 进度值 | 动态显示 0-100% 数值 |

**底部操作按钮**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 进入详情 | 关闭 Modal 并跳转到对应模块页面 |

---

## 四、数据服务

> 数据服务模块提供数据集的统一管理能力，支持训练数据、测试数据和推理结果的管理。

### 4.1 训练数据管理

#### 4.1.1 功能说明
- **说明**：管理训练用数据集，支持多种数据格式（jsonl / xlsx 等），支持本地上传和存储桶导入两种方式
- **URL**：`/datasets`

#### 4.1.2 列表字段

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本状态 | 版本发布状态（已发布/草稿/已归档），带颜色标签 |
| 最新版本 | 版本号 |
| 数据用途 | 用途类型（文本生成/图像理解），蓝色标签 |
| 数据格式 | 数据格式（jsonl / xlsx / PROMPT_RESPONSE / ROLE_BASED 等） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 查看详情、删除 |

> **注意**：列表支持行展开，展开后显示该数据集下所有版本的信息（版本号、字符数、样本数、训练比例等）

#### 4.1.3 列表功能
- **搜索**：按数据集名称搜索
- **创建数据集**：打开创建数据集弹窗
- **刷新**：刷新当前列表
- **行展开**：点击行展开箭头，显示该数据集下所有版本的详细信息（版本号、字符数、样本数、训练比例等）
- **查看详情**：点击后弹出详情 Modal

#### 4.1.4 详情 Modal

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据集名称 | 输入框 | 是 | 支持中英文、数字、下划线、中划线；2-64 个字符 |
| 数据集版本 | 输入框 | 否 | 自动生成，默认 V1（禁用状态） |
| 描述 | 文本域 | 否 | 最多 300 字符，显示字数统计 |

**数据配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据用途 | 下拉选择 | 是 | 训练 / 图像理解 |
| 数据属性 | 下拉选择 | 否 | 数据属性标签 |
| 数据格式 | 下拉选择 | 是 | PROMPT_RESPONSE（Prompt-Response 对格式）/ ROLE_BASED（角色对话格式）/ jsonl / xlsx / csv |

**数据上传**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据来源 | 下拉选择 | 是 | 本地上传 / 存储桶导入 |
| 上传文件 | 文件上传 | 是 | 支持拖拽上传；支持 .jsonl / .json / .xlsx；单个文件最大 100MB |

**交互流程**：
1. 用户填写基本信息（必填项）
2. 选择数据用途和数据格式
3. 选择数据来源（本地/存储桶）
4. 上传数据文件
5. 系统实时显示上传进度（0-100% 进度条）
6. 上传完成后显示文件列表，支持删除重选
7. 点击"创建"按钮提交

#### 4.1.5 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该数据集的完整信息：

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本状态 | 版本发布状态（已发布/草稿/已归档），带颜色标签 |
| 最新版本 | 版本号 |
| 数据用途 | 用途类型（文本生成/图像理解） |
| 数据格式 | 数据格式（jsonl / xlsx 等） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

---

### 4.2 测试数据管理

#### 4.2.1 功能说明
- **说明**：管理测试用数据集，用于模型评估和验证，支持本地上传和存储桶导入
- **URL**：`/measurement`

#### 4.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本状态 | 版本发布状态 |
| 最新版本 | 版本号 |
| 数据用途 | 用途类型（测试） |
| 数据格式 | 数据格式 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 查看详情、删除 |

#### 4.2.3 列表功能
- **搜索**：按数据集名称搜索
- **创建数据集**：打开创建数据集弹窗
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 4.2.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该数据集的完整信息：

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本状态 | 版本发布状态 |
| 最新版本 | 版本号 |
| 数据用途 | 用途类型（测试） |
| 数据格式 | 数据格式 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 4.2.5 创建数据集表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据集名称 | 输入框 | 是 | 2-64 字符，支持中英文、数字、下划线、中划线 |
| 描述 | 文本域 | 否 | 最多 300 字符 |
| 数据用途 | 下拉选择 | 是 | 固定为"测试" |
| 数据属性 | 下拉选择 | 否 | 数据属性标签 |
| 数据格式 | 下拉选择 | 是 | jsonl / xlsx / csv |
| 数据来源 | 下拉选择 | 是 | 本地上传 / 存储桶导入 |
| 上传文件 | 文件上传 | 是 | 支持拖拽；.jsonl / .json / .xlsx / .csv；最大 100MB |

---

### 4.3 推理结果集

#### 4.3.1 功能说明
- **说明**：管理离线推理任务产生的推理结果数据，适用于模型选型、效果评估或模型复用场景
- **URL**：`/inference`

#### 4.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 推理进度 | 推理完成百分比（%），带颜色进度条展示 |
| 数据用途 | 用途类型（文本生成/图像理解） |
| 待推理数据 | 输入数据集名称 |
| 待推理模型/服务 | 使用的模型或服务 |
| 数据量 | 数据条数 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、编辑、删除 |

#### 4.3.3 列表功能
- **搜索**：按数据集名称搜索
- **创建数据集**：打开创建数据集弹窗
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 4.3.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该数据集的完整信息：

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 推理进度 | 推理完成百分比（%），带颜色进度条展示 |
| 数据用途 | 用途类型（文本生成/图像理解） |
| 待推理数据 | 输入数据集名称 |
| 待推理模型/服务 | 使用的模型或服务 |
| 数据量 | 数据条数 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 4.3.5 创建推理数据集表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据集名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**推理配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 待推理数据 | 下拉选择 | 是 | 从训练数据集列表中选择 |
| 待推理模型/服务 | 下拉选择 | 是 | 从已部署/已训练模型中选择 |
| 数据量 | 数字输入 | 是 | 正整数，单位：条 |

**任务配置（可选）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务定时配置 | 开关 + 时间选择器 | 否 | 开关关闭时隐藏时间选择器；开关打开后可选择日期时间，格式：YYYY-MM-DD HH:mm |

**业务提示**：
> 推理任务创建后将自动进入排队状态，等待资源分配后开始执行。

**状态说明**：

| 状态 | 说明 |
|------|------|
| 已创建 | 任务已创建，等待启动 |
| 排队中 | 任务正在等待 K8s 资源分配 |
| 运行中 | 推理正在进行 |
| 已完成 | 推理成功完成 |
| 失败 | 推理失败 |

---

## 五、数据处理

### 5.1 功能架构

```
数据处理
├── 数据标注
└── 数据清洗
```

### 5.2 数据标注

#### 5.2.1 功能说明
- **说明**：对原始数据进行标注处理，生成高质量训练数据。支持在线标注和多人协同标注
- **URL**：`/data-annotation`

#### 5.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 任务名称 | 标注任务名称 |
| 数据量 | 数据条数 |
| 标注进度 | 标注完成比例（如 850/1000） |
| 标注前数据集 | 原始数据集名称 |
| 标注后数据集 | 标注完成后的数据集名称 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 查看详情、删除 |

#### 5.2.3 列表功能
- **创建标注任务**：打开创建弹窗
- **刷新**：刷新当前列表
- **开始标注**：对未开始的标注任务，点击后可进入标注页面开始标注
- **查看详情**：点击后弹出详情 Modal

#### 5.2.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该标注任务的完整信息：

| 字段 | 说明 |
|------|------|
| 任务名称 | 标注任务名称 |
| 数据量 | 数据条数 |
| 标注进度 | 标注完成比例（如 850/1000） |
| 标注前数据集 | 原始数据集名称 |
| 标注后数据集 | 标注完成后的数据集名称 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 开始标注 | 对未开始的标注任务，点击后可进入标注页面开始标注 |

#### 5.2.5 创建标注任务表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**数据配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据集类型 | 下拉选择 | 是 | SFT 文本生成 / VLM 图像理解 |
| 数据选择 | 下拉选择（带搜索） | 是 | 根据数据集类型筛选可用的训练数据集 |

> 选择数据集后，显示数据集类型和数据集条数的提示信息。

**输出配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 处理后数据集 | 单选 | 是 | 新增版本（在原数据集上新建版本）/ 新建数据集 |
| 数据集名称 | 输入框 | 是（当选择"新建数据集"时） | 最多 64 字符，说明文字："标注完成后的数据集名称" |

**任务配置（可选）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务定时配置 | 开关 + 时间选择器 | 否 | 定时启动标注任务 |

**交互流程**：
1. 选择数据集类型（系统据此过滤可选数据集）
2. 选择待标注数据集
3. 选择输出方式（追加版本或新建）
4. 选择是否启用定时
5. 点击"创建"提交

---

### 5.3 数据清洗

#### 5.3.1 功能说明
- **说明**：对原始数据进行清洗处理，去除噪声和无效数据，提高数据质量
- **URL**：`/data-cleaning`

#### 5.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 任务名称 | 清洗任务名称 |
| 目标数据集 | 被清洗的数据集名称 |
| 清洗规则 | 应用的清洗规则描述 |
| 清洗数量/总数 | 已清洗条数 / 总条数，带颜色进度条 |
| 状态 | pending（待清洗，蓝色）/ in_progress（清洗中，蓝色）/ completed（已完成，绿色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 执行、详情、删除 |

#### 5.3.3 列表功能
- **创建清洗任务**：打开创建弹窗
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 5.3.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该清洗任务的完整信息：

| 字段 | 说明 |
|------|------|
| 任务名称 | 清洗任务名称 |
| 目标数据集 | 被清洗的数据集名称 |
| 清洗规则 | 应用的清洗规则描述 |
| 清洗数量/总数 | 已清洗条数 / 总条数，带颜色进度条 |
| 状态 | pending（待清洗，蓝色）/ in_progress（清洗中，蓝色）/ completed（已完成，绿色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 5.3.5 创建清洗任务表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**数据配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据来源 | 下拉选择 | 是 | 从已上传的训练数据集中选择 |
| 清洗后数据集名称 | 输入框 | 是 | 最多 64 字符 |

**清洗能力配置**（多选，按类别分组）：

| 类别 | 清洗能力选项 |
|------|-------------|
| 数据格式清洗 | 空白字符清洗、乱码清洗、HTML 标签清洗、多余换行符清洗 |
| LLM 生成数据清洗 | 长度异常过滤器、重复内容移除器、截断句移除器、语种过滤器 |
| 数据去重 | 精确匹配去重、MinHash 去重、SimHash 去重 |
| 敏感数据清洗 | 联系方式过滤器、证件号码过滤器、网络地址过滤器、金融车辆过滤器、社交账号过滤器、自定义关键词过滤器 |

> 选中能力后显示已选数量徽标提示。

**任务配置（可选）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务定时配置 | 开关 + 时间选择器 | 否 | 定时启动清洗任务 |

**交互流程**：
1. 填写任务名称
2. 选择数据来源和输出数据集名称
3. 在清洗能力中勾选所需规则（至少选一项）
4. 选择是否启用定时
5. 点击"创建"提交

---

## 六、模型训练

### 6.1 功能架构

```
模型训练
├── 在线Notebook
├── 大模型训练
│   ├── 训练流程说明（步骤卡片）
│   ├── 训练任务列表
│   │   ├── 任务名称
│   │   ├── 版本数量
│   │   ├── 创建时间
│   │   └── 操作（查看详情/删除）
│   └── 创建训练任务
│       ├── 基础配置
│       ├── 模型配置（支持基础模型/我的模型来源切换）
│       ├── 训练配置（支持 SFT / DPO / RM / PPO 训练方法）
│       │   ├── 基础参数
│       │   ├── 高级配置
│       │   ├── 数据处理配置
│       │   ├── LoRA 配置（若选择 LoRA 微调）
│       │   ├── 评估配置
│       │   ├── 保存配置
│       │   └── 监控配置
│       ├── 数据配置
│       └── 显卡资源配置
└── 模型管理
```

### 6.2 训练任务管理流程

```
数据准备 → 任务创建 → 配置参数 → 分布式训练 → 训练记录
```

1. **数据准备**：准备训练所需数据集（训练数据集 + 验证数据集）
2. **任务创建**：创建新的模型训练任务
3. **配置参数**：设置模型和训练参数
4. **分布式训练**：任务提交后，多 GPU 集群高效训练
5. **训练记录**：查看和管理历史训练版本

---

### 6.3 训练任务列表

#### 6.3.1 列表字段

| 字段 | 说明 |
|------|------|
| **训练任务名称** | 任务的名称标识 |
| **版本数量** | 该任务下的训练版本数量 |
| **创建时间** | 任务创建时间（格式：YYYY/MM/DD HH:mm:ss） |
| **操作** | 查看详情、删除 |

#### 6.3.2 列表功能

- **刷新**：刷新当前列表
- **创建训练任务**：打开创建训练任务弹窗

#### 6.3.3 任务详情页

点击任务名称或"查看详情"进入任务详情页，包含：
- 任务基本信息（任务名称、基础模型、训练类型、训练方法）
- 版本列表（多版本管理）

---

### 6.4 创建训练任务

#### 6.4.1 基础配置

| 字段 | 说明 | 约束 |
|------|------|------|
| 任务名称 | 训练任务的名称 | 必填，新建版本时不可修改 |
| 任务版本 | 任务的版本号 | 默认 V1，新建版本时自动递增（V2、V3...） |
| 任务定时配置 | 可选配置，支持定时启动训练任务 | 开关控制：关闭时不显示时间选择器；打开后可选择日期时间（YYYY-MM-DD HH:mm） |
| 任务描述 | 任务的详细描述 | 最多 300 字符，显示字数统计 |

**定时配置交互逻辑**：
- 页面加载时定时开关默认关闭
- 开关打开后，右侧展开 DatePicker 时间选择器
- 开关关闭后，时间选择器自动隐藏，表单中不包含时间值
- 定时任务到达指定时间后自动启动训练

#### 6.4.2 模型配置

**选择训练类型**：

| 训练类型 | 说明 |
|----------|------|
| **文本生成** | 适用于文本生成类大语言模型（LLM）的训练 |
| **图像理解** | 适用于视觉语言模型（VLM）的训练，支持图像+文本多模态 |

> **重要**：切换训练类型后，将重新筛选可用的具体模型版本，并清空已选模型。

**模型来源切换**：

| 来源 | 说明 |
|------|------|
| **基础模型** | 从平台预置的基础模型列表中选择（默认选项） |
| **我的模型** | 从已训练完成的模型中选择，用于强化学习续训 |

> **重要**：切换模型来源后，将清空已选模型。

**基础模型来源**（默认）：

| 字段 | 说明 |
|------|------|
| 选择基础模型系列 | 卡片式选择器，当前仅支持 Qwen 系列 |
| 基础模型版本 | 标签式选择器，根据训练类型动态筛选显示，按系列分组（Qwen2.5 系列 / Qwen3 / Qwen3.5 系列 / 语音与重排） |

**我的模型来源**：
- 仅显示与当前训练类型匹配的已训练模型
- 卡片式列表展示，显示模型名称、版本数量、训练方法标签
- 若无匹配的已训练模型，显示空状态提示："当前训练类型暂无已训练模型"

**我的模型过滤逻辑**：`trainedModels.filter(m => m.type === trainingType)`

**基础模型版本过滤逻辑**：`baseModelVariants.filter(v => v.type === trainingType && v.seriesId === selectedSeries)`

**平台支持的基础模型列表**：

| 模型名称 | 类型 | 说明 |
|----------|------|------|
| Qwen2-VL-2B-Instruct | 图像理解 | 视觉语言模型 |
| Qwen2.5-0.5B-Instruct-GPTQ-Int8 | 文本生成 | GPTQ Int8 量化优化版本 |
| Qwen2.5-0.5B-Instruct | 文本生成 | 轻量级指令模型 |
| Qwen2.5-0.5B-Instruct-AWQ | 文本生成 | AWQ 量化版本 |
| Qwen2.5-1.5B-Instruct | 文本生成 | 小型指令模型 |
| Qwen2.5-7B-Instruct | 文本生成 | 中型指令模型 |
| Qwen2.5-32B-Instruct | 文本生成 | 大型指令模型 |
| Qwen3-0.6B-FP8 | 文本生成 | FP8 精度版本 |
| Qwen3-0.6B-GGUF | 文本生成 | GGUF 量化版本 |
| Qwen3-1.7B | 文本生成 | 基础语言模型 |
| Qwen3-1.7B-FP8 | 文本生成 | FP8 精度版本 |
| Qwen3-1.7B-GPTQ-Int8 | 文本生成 | GPTQ Int8 量化版本 |
| Qwen3-1.7B-MLX-6bit | 文本生成 | MLX 6bit 量化 |
| Qwen3-1.7B-MLX-8bit | 文本生成 | MLX 8bit 量化 |
| Qwen3-4B | 文本生成 | 小型语言模型 |
| Qwen3-8B | 文本生成 | 中型语言模型 |
| Qwen3-8B-GGUF | 文本生成 | GGUF 量化版本 |
| Qwen3-VL-8B-Instruct | 图像理解 | 视觉语言模型 |
| Qwen3-ASR-0.6B | 文本生成 | ASR 语音识别模型 |
| Qwen3-Reranker-0.6B | 文本生成 | 重排序模型 |
| Qwen3.5-4B | 文本生成 | Qwen3.5 系列 |

**训练方法**：

| 训练方法 | 说明 | 适用微调类型 |
|----------|------|-------------|
| **SFT**（监督微调） | 在预训练模型基础上使用标注数据进行的监督微调 | 全参微调 / LoRA 微调 |
| **DPO**（直接偏好优化） | 强化学习方法，直接优化语言模型对齐人类偏好 | 全参微调 / LoRA 微调 |
| **RM**（奖励模型训练） | 训练一个奖励模型来评估生成质量 | 仅全参微调 |
| **PPO**（近端策略优化） | 强化学习方法，使用奖励模型优化策略 | 全参微调 / LoRA 微调 |

**微调类型**（仅 SFT / DPO / PPO 可选）：

| 微调类型 | 说明 | 适用场景 |
|----------|------|----------|
| **全参微调** | 更新模型所有参数 | 追求最佳效果，资源充足 |
| **LoRA 微调** | Low-Rank Adaptation，仅更新少量参数 | 资源受限，需快速迭代 |

> **重要**：切换微调类型后，表单将自动滚动到"基础参数"标签页，并重置相关配置。

**PPO 强化学习链路**：
> PPO 训练需要奖励模型（RM），必须先训练 RM 模型后方可进行 PPO 训练。若无可用的 RM 奖励模型，下拉选择框将显示禁用状态并提示"暂无可用的奖励模型，请先训练 RM 模型"。

**奖励模型过滤逻辑**：`trainedModels.filter(m => m.type === trainingType && m.method === 'RM')`

---

### 6.5 训练配置详情

#### 6.5.1 基础参数（标签页 1）

| 字段 | 默认值 | 说明 | 提示 |
|------|--------|------|------|
| 学习率 | 0.000050 | 控制模型学习新知识的速度 | 过高的会导致训练不稳定，过低会使训练速度过慢 |
| 训练轮次 | 3 | 控制训练过程中遍历过数据集合的次数 | 建议范围：1-15 之间 |
| 每个设备上的训练 batch 大小 | 2 | 控制每个设备上进行训练时的批次大小 | 影响训练速度和内存占用 |
| 梯度累积步数 | 1 | 控制梯度累积的步数 | 影响训练速度和内存占用 |
| 预热比例 | 0.1 | 训练开始时学习率逐渐增加到设定值的比例占总训练步数的比例 | 帮助训练初期稳定收敛 |
| 学习率调度器类型 | COSINE | 自动学习率调度器 | 常用值：COSINE（余弦退火）/ LINEAR / CONSTANT |
| 是否使用 bf16 精度 | 是 | 使用 bf16 精度可以提高训练速度 | 默认开启 |

#### 6.5.2 高级配置（标签页 2）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 梯度检查点 | 否 | 通过梯度检查点技术减少训练过程中的内存占用，适用于显存受限的情况 |
| 最大梯度范数 | 1.0 | 梯度裁剪有助于稳定训练过程，防止梯度爆炸问题 |
| RoPE 缩放方法 | YARN | RoPE 缩放方法用于扩展模型的上下文窗口大小 |
| 随机种子 | 42 | 设置固定的随机种子可以确保训练过程的可重复性 |
| 权重衰减 | 0 | 正则化技术，有助于防止模型过拟合，设置为 0 表示不使用 |

#### 6.5.3 数据处理配置（标签页 3）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 训练样本的最大 token 长度限制（Cutoff Len） | 4096 | 影响训练效果和内存占用，范围：64-8192 |
| 预处理进程数（Preprocessing Num Workers） | 16 | 控制预处理各种进程数，影响数据加载速度 |

#### 6.5.4 评估配置（标签页 4）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 评估策略 | STEPS | 控制模型评估的频率和时机（按步数 / 按轮次） |
| 评估间隔步数 | 20 | 每训练指定步数后进行一次模型评估 |
| 指标越大越好 | 否 | 控制评估指标的优化方向 |
| 训练结束加载最佳模型 | 是 | 开启后，训练结束时会自动加载评估表现最佳的模型权重 |
| 最佳模型指标 | 损失值 (loss) | 用于判断训练过程中最佳模型的评估指标（损失值 / 准确率） |
| 每个设备上的评估 batch 大小 | 2 | 控制评估时的批次大小 |

#### 6.5.5 保存配置（标签页 5）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 模型保存策略 | STEPS | 控制模型保存的频率和时机（按步数 / 按轮次） |
| 模型保存步数 | 20 | 每训练指定步数后进行一次模型保存 |
| 模型保存总数限制 | 3 | 限制保存的模型版本总数，用于控制存储空间使用 |

#### 6.5.6 监控配置（标签页 6）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 日志记录频率 | 5 | 控制训练日志的记录频率，便于问题排查和训练过程分析 |

#### 6.5.7 LoRA 配置（仅选择 LoRA 微调时显示标签页 7）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| LoRA 秩 (rank) | 16 | LoRA 的秩，越大效果越好但占用更多资源（范围：1-256） |
| LoRA 目标模块 | all | 应用 LoRA 的目标模块名称（all / q_proj / k_proj / v_proj / o_proj / gate_proj / up_proj / down_proj），支持多选 |
| LoRA alpha 参数 | 32 | LoRA 的 alpha 参数，控制适配器权重比例 |
| LoRA dropout 率 | 0 | LoRA 层的 Dropout 率，防止过拟合 |

**LoRA 目标模块互斥逻辑**：
- 若选中 `all`，则不允许再选择具体模块
- 若选中具体模块，则自动清除 `all`
- 每次选择变化时自动处理互斥关系

---

### 6.6 数据配置

#### 6.6.1 训练数据集

**数据集选择器交互**：
1. 点击"添加数据集"按钮，打开数据集选择弹窗
2. 在弹窗中根据训练类型（文本生成/图像理解）和训练方法（SFT/DPO/RM/PPO）自动筛选可用数据集
3. 选择数据集版本，设置训练比例
4. 点击确认，数据集添加到列表中
5. 可多次添加多个数据集

**数据集表格字段**：

| 字段 | 说明 |
|------|------|
| 训练数据集 | 数据集名称 |
| 版本 | 数据集版本 |
| 字符数 | 数据集字符总数（千分位格式化） |
| 样本数 | 数据样本数量（千分位格式化） |
| 采样率 | 数据采样比例 |
| 训练比例 (%) | 用于训练的数据比例 |
| 操作 | 删除（从列表中移除） |

**数据集比例要求**：总训练比例必须等于 100%，否则提示红色警告文字。

**总览提示**（当数据集数量 > 0 时显示）：
> 总记录数：X 条
> 总比例：X%（必须等于 100%）

#### 6.6.2 验证集配置

| 字段 | 说明 |
|------|------|
| 验证集配置 | 切换验证集来源方式：数据拆分 / 验证数据集 |
| 拆分比例 | 仅在"数据拆分"模式下显示，滑动选择器：0% / 5% / 10% / 15% / 20%，默认 15% |
| 验证数据集 | 仅在"验证数据集"模式下显示，打开数据集选择弹窗 |

---

### 6.7 显卡资源配置

| 字段 | 可选值 | 说明 |
|------|--------|------|
| 显卡类型及型号 | NVIDIA A100 / V100 / T4 / H100 | 选择 GPU 型号 |
| 显卡卡数配置 | 1卡 / 2卡 / 4卡 / 8卡 | 选择 GPU 数量 |

**资源配额**：

| 资源类型 | 单位 | 说明 |
|----------|------|------|
| CPU 请求 | Core | 最低 CPU 资源请求 |
| CPU 限制 | Core | CPU 上限资源 |
| 内存请求 | GB | 最低内存请求 |
| 内存限制 | GB | 内存上限资源 |

---

### 6.8 任务版本管理

#### 6.8.1 版本列表字段

| 字段 | 说明 |
|------|------|
| 版本 | 版本标识（V1、V2、V3...） |
| 描述 | 版本描述 |
| 运行状态 | 任务运行状态 |
| 运行时长 | 已运行时长 |
| GPU 数量 | 使用的 GPU 数量 |
| 创建人 | 版本创建者 |
| 创建时间 | 版本创建时间 |
| 操作 | 启动、编辑、删除 |

#### 6.8.2 运行状态与流转

```
已创建（定时待启动）
    ↓
[到达定时时间或手动启动]
    ↓
启动中（数据预处理）
    ↓
排队中（等待 K8s 资源分配）
    ↓
运行中（训练正在进行）
    ↓
已完成（训练成功）/ 失败（训练失败）
        ↓
[可在任意排队或运行状态手动终止]
    ↓
终止
```

| 状态 | 说明 |
|------|------|
| **已创建 / 定时待启动** | 任务已创建，未启动或等待定时时间到达 |
| **启动中** | 任务已启动，正在做数据预处理 |
| **排队中** | 任务正在排 K8s 资源，若排到资源将进入运行中状态 |
| **运行中** | 训练正在进行 |
| **已完成** | 训练成功完成 |
| **失败** | 训练失败 |
| **终止** | 对排队中或运行中的任务，可进行终止操作，状态变为终止 |

#### 6.8.3 版本操作

| 操作 | 说明 |
|------|------|
| **启动** | 启动训练任务（若已配置定时，则在定时时间自动启动） |
| **编辑** | 重新编辑训练配置参数 |
| **删除** | 删除该版本（仅未启动的版本可删除） |
| **日志** | 查看训练任务实时日志 |
| **详情** | 查看训练任务详细信息 |
| **终止** | 终止正在排队或运行中的训练任务，释放资源 |

#### 6.8.4 新增版本

- **操作**：点击"新增版本"按钮，基于当前任务的最新配置创建新版本
- **说明**：新版本自动继承父任务的训练类型和训练方法；任务名称不可修改；版本号自动递增
- **新建版本时**：默认从基础模型选择，也可手动切换为"我的模型"

---

### 6.9 在线 Notebook

#### 6.9.1 功能说明
- **说明**：交互式编程环境，支持 Python / Jupyter，提供即开即用的数据科学环境
- **URL**：`/finetune/notebooks`

#### 6.9.2 Tab 切换

页面支持两个 Tab 切换：

| Tab | 说明 |
|-----|------|
| 我的 Notebook | 用户个人创建的 Notebook 列表 |
| Notebook 广场 | 平台预置的模板案例，可复制使用 |

#### 6.9.3 列表字段

**我的 Notebook**：

| 字段 | 说明 |
|------|------|
| Notebook 名称 | Notebook 标识名称 |
| 状态 | 运行中（绿色）/ 已停止（灰色） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 打开、详情 |

**Notebook 广场**：

| 字段 | 说明 |
|------|------|
| 案例名称 | 模板案例名称 |
| 描述 | 模板案例描述 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 复制案例、详情 |

#### 6.9.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该 Notebook 的完整信息：

| 字段 | 说明 |
|------|------|
| 名称 | Notebook 名称 |
| 状态（我的 Notebook） | 运行中 / 已停止 |
| 描述（Notebook 广场） | 模板案例描述 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| Tab | 按钮 | 说明 |
|-----|------|------|
| 我的 Notebook | 打开 | 关闭 Modal 并打开 Notebook 环境 |
| Notebook 广场 | 复制案例 | 关闭 Modal 并复制模板案例 |

#### 6.9.5 创建 Notebook 表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| Notebook名称 | 输入框 | 是 | 支持中英文、数字、下划线、中划线，2-64 字符 |
| 描述 | 文本域 | 否 | Notebook 描述（可选），最多 300 字符 |

---

### 6.10 模型管理

#### 6.10.1 功能说明
- **说明**：统一管理所有训练产生的模型，支持版本追溯和快速部署配置
- **URL**：`/model`

#### 6.10.2 列表字段

| 字段 | 说明 |
|------|------|
| 模型名称 | 模型标识名称 |
| 模型类型 | 模型分类标签（文本生成 / 图像理解），蓝色标签 |
| 基础模型 | 模型的基础来源 |
| 版本数量 | 版本数量（X 个版本） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 查看详情、删除 |

#### 6.10.3 列表功能
- **搜索**：按模型名称搜索
- **刷新**：刷新当前列表
- **创建模型**：打开创建模型弹窗
- **查看详情**：点击后弹出详情 Modal

#### 6.10.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该模型的完整信息：

| 字段 | 说明 |
|------|------|
| 模型名称 | 模型标识名称 |
| 模型类型 | 模型分类标签（文本生成 / 图像理解） |
| 基础模型 | 模型的基础来源 |
| 版本数量 | 版本数量（X 个版本） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 6.10.5 创建模型表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 模型名称 | 输入框 | 是 | 支持中英文、数字、下划线、中划线，2-64 字符 |
| 描述 | 文本域 | 否 | 模型描述（可选），最多 200 字符 |

**模型配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 模型来源 | 下拉选择 | 是 | 大模型训练 / Notebook |
| 模型类型 | 下拉选择 | 是 | 文本生成 / 图像生成 / 多模态 |
| 基础模型 | 下拉选择 | 是 | 从可用基础模型列表中选择 |

---

## 七、模型评估

> 模型评估模块提供大模型效果评估能力，支持效果评估和自定义评估指标。

### 7.1 功能架构

```
模型评估
├── 效果评估
└── 评估指标
```

---

### 7.2 效果评估

#### 7.2.1 功能说明
- **说明**：对训练完成的模型进行效果评估，量化模型性能，支持裁判模型评估和基础指标评估两种方式
- **URL**：`/effect-evaluation`

#### 7.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 任务名称 | 评估任务标识 |
| 任务状态 | pending（待评估，蓝色）/ running（评估中，蓝色）/ completed（已完成，绿色）/ failed（评估失败，红色） |
| 运行时长 | 已运行时长 |
| 评估进度 | 评估完成百分比（%），带颜色进度条 |
| 推理结果集 | 用于评估的推理结果数据集 |
| 待评估模型/服务 | 被评估的模型或服务 |
| 评估方法 | 评估方法（裁判员评估 / 基础指标评估） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、编辑、删除 |

#### 7.2.3 评估状态

| 状态 | 说明 |
|------|------|
| pending | 待评估，任务已创建但未启动 |
| running | 评估中，评估任务正在进行 |
| completed | 已完成，评估成功结束 |
| failed | 评估失败，评估过程中出错 |

#### 7.2.4 列表功能
- **新建评估**：打开创建评估任务弹窗
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 7.2.5 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该评估任务的完整信息：

| 字段 | 说明 |
|------|------|
| 任务名称 | 评估任务标识 |
| 任务状态 | pending（待评估，蓝色）/ running（评估中，蓝色）/ completed（已完成，绿色）/ failed（评估失败，红色） |
| 运行时长 | 已运行时长 |
| 评估进度 | 评估完成百分比（%），带颜色进度条 |
| 推理结果集 | 用于评估的推理结果数据集 |
| 待评估模型/服务 | 被评估的模型或服务 |
| 评估方法 | 评估方法（裁判员评估 / 基础指标评估） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 7.2.6 创建评估任务表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 评估任务名称 | 输入框 | 是 | 2-64 字符 |
| 评估类别 | 下拉选择 | 是 | SFT 文本生成 / VLM 图像理解 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**评估配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 评估数据来源 | 单选 | 是 | 已有推理结果集 / 新建（当前仅支持"已有推理结果集"） |
| 待评估推理结果集 | 下拉选择 | 是 | 从已有的推理结果数据集中选择 |
| 待评估模型/服务 | 下拉多选 | 是 | 从已部署的模型中选择，可多选以支持对比评估 |
| 评估方法 | 下拉选择 | 是 | 裁判员评估 / 基础指标评估 |

**裁判员评估**（当评估方法选择"裁判员评估"时显示）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 裁判模型/服务 | 下拉选择 | 是 | 选择用于评判生成质量的模型或服务 |

**评估指标管理**：

> 点击各指标类型的"添加"按钮，可将指标加入评估列表。每个指标可设置评分说明。

| 指标名称 | 说明 | 评分范围 | 评分说明示例 |
|----------|------|---------|------------|
| 答案相关性 | 评估回答与问题的相关程度 | 0-10 | 0分表示完全无关，10分表示高度相关 |
| 忠实度 | 评估回答对原始文档的忠实程度 | 0-10 | 0分表示完全不忠实，10分表示完全忠实 |
| 上下文精确度 | 评估回答对上下文的引用精确度 | 0-10 | 0分表示完全不精确，10分表示完全精确 |
| 上下文召回率 | 评估回答对关键信息的召回程度 | 0-10 | 0分表示未召回任何关键信息，10分表示完全召回 |
| 上下文相关性 | 评估回答与上下文的整体相关性 | 0-10 | 0分表示完全无关，10分表示完全相关 |

**已选指标表格字段**：

| 字段 | 说明 |
|------|------|
| 指标名称 | 指标显示名称 |
| 操作 | 删除（从已选列表中移除） |

**任务配置（可选）**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务定时配置 | 开关 + 时间选择器 | 否 | 定时启动评估任务 |

---

### 7.3 评估指标

#### 7.3.1 功能说明
- **说明**：管理自定义评估指标，支持指标的新增、编辑和删除
- **URL**：`/evaluation-indicator`

#### 7.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 指标名称 | 指标标识名称 |
| 指标类型 | 指标分类（蓝色文本） |
| 指标描述 | 指标说明（超长时省略） |
| 计算方式 | 计算逻辑描述（代码样式） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 编辑、删除 |

#### 7.3.3 列表功能
- **新增指标**：打开创建指标弹窗
- **搜索**：按指标名称搜索
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 7.3.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该指标的完整信息：

| 字段 | 说明 |
|------|------|
| 指标名称 | 指标标识名称 |
| 指标类型 | 指标分类（蓝色文本） |
| 指标描述 | 指标说明 |
| 计算方式 | 计算逻辑描述（代码样式） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 7.3.5 创建指标表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 指标名称 | 输入框 | 是 | 2-64 字符 |
| 指标类型 | 下拉选择 | 是 | 翻译质量 / 摘要质量 / 分类准确率 / 综合指标 / 语言模型 / 答案相关性 / 忠实度 |
| 计算方式 | 下拉选择 | 是 | BLEU / ROUGE-L / Accuracy / F1-Score / Perplexity / 自定义 |
| 指标说明 | 文本域 | 是 | 描述指标的计算逻辑和使用方法 |

---

## 八、模型服务

> 模型服务模块提供模型部署和在线推理能力。

### 8.1 功能架构

```
模型服务
├── 模型部署
└── 在线推理服务
```

---

### 8.2 模型部署

#### 8.2.1 功能说明
- **说明**：将训练完成的模型部署为在线服务，支持 vLLM 和 DGI Server 两种推理引擎
- **URL**：`/service/inference/hosted`

#### 8.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 服务名称 | 部署服务名称 |
| 模型名称 | 部署的模型名称 |
| 模型来源 | 训练生成 / 基础模型 |
| 实例数 | 服务实例数量 |
| 状态 | running（运行中，绿色）/ stopped（已停止，灰色）/ error（服务异常，红色） |
| 创建人 | 部署者 |
| 创建时间 | 部署时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、停止、详情、删除 |

#### 8.2.3 服务状态

| 状态 | 说明 |
|------|------|
| running | 服务运行中 |
| stopped | 服务已停止 |
| error | 服务异常 |

#### 8.2.4 列表功能
- **部署服务**：打开创建部署弹窗
- **搜索**：按服务名称或模型名称搜索
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 8.2.5 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该部署服务的完整信息：

| 字段 | 说明 |
|------|------|
| 服务名称 | 部署服务名称 |
| 模型名称 | 部署的模型名称 |
| 模型来源 | 训练生成 / 基础模型 |
| 实例数 | 服务实例数量 |
| 状态 | running（运行中，绿色）/ stopped（已停止，灰色）/ error（服务异常，红色） |
| 创建人 | 部署者 |
| 创建时间 | 部署时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 启动 | 对已停止的服务，点击后启动服务（running 状态下禁用） |
| 停止 | 对运行中的服务，点击后停止服务（stopped 状态下禁用） |

#### 8.2.6 创建部署服务表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 服务名称 | 输入框 | 是 | 2-64 字符 |

**模型配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 模型来源 | 单选 | 是 | 训练生成 / 基础模型 |
| 选择模型 | 下拉选择 | 是 | 根据模型来源筛选对应模型列表 |

> 选择模型后，显示模型来源和模型类型的提示信息。

**资源信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| CPU 请求 | 数字输入 | 是 | 单位：Core |
| CPU 限制 | 数字输入 | 是 | 单位：Core |
| 内存请求 | 数字输入 | 是 | 单位：GB |
| 内存限制 | 数字输入 | 是 | 单位：GB |
| 显卡类型 | 下拉选择 | 是 | NVIDIA A100 / V100 / T4 / H100 |
| 显卡数量 | 下拉选择 | 是 | 1卡 / 2卡 / 4卡 / 8卡 |
| 部署实例数 | 数字输入 | 是 | 正整数 |

**镜像配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 镜像类型 | 下拉选择 | 是 | vLLM / DGI Server |
| 选择镜像 | 下拉选择 | 是 | 从可用镜像列表中选择 |
| 运行命令 | 文本域 | 否 | 自定义启动命令 |

---

### 8.3 在线推理服务

#### 8.3.1 功能说明
- **说明**：查看和管理已部署的在线推理服务，支持实时调用模型进行推理
- **URL**：`/service/inference/external`

#### 8.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 服务名称 | 推理服务名称 |
| 模型名称 | 推理模型名称 |
| 模型来源 | 训练生成 / 基础模型 |
| 实例数 | 实例数量 |
| 状态 | running（运行中，绿色）/ stopped（已停止，灰色），带颜色标签 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、停止 |

#### 8.3.3 服务状态

| 状态 | 说明 |
|------|------|
| running | 服务运行中（绿色标签），停止按钮可用 |
| stopped | 服务已停止（灰色标签），启动按钮可用 |

#### 8.3.4 列表功能
- **刷新**：刷新当前列表
- **启动**：启动已停止的服务（仅 stopped 状态可用）
- **停止**：停止运行中的服务（仅 running 状态可用）

---

## 九、机器学习

> 机器学习模块提供传统机器学习相关的数据管理和标注功能。

### 9.1 功能架构

```
机器学习
├── 数据管理
├── 机器学习标注
├── 模型管理
├── 模型部署
└── 在线Notebook
```

---

### 9.2 数据管理

> **说明**：机器学习模块的数据管理，与机器学习训练产生的模型管理有所区别。

#### 9.2.1 功能说明
- **说明**：管理和创建用于机器学习的数据集，支持数据查看、导入导出和删除等操作
- **URL**：`/machine-data-management`

#### 9.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本 | 版本号 |
| 数据类型 | 图像 / 文本 / 音频 |
| 标注类型 | 文本分类 / 图像分类 / 图像分割 / 物体检测 等 |
| 标注模板 | 使用的数据标注模板 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 查看详情、删除 |

#### 9.2.3 列表功能
- **创建数据集**：打开创建数据集弹窗
- **搜索**：按标注类型筛选
- **刷新**：刷新当前列表
- **查看详情**：点击后弹出详情 Modal

#### 9.2.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该数据集的完整信息：

| 字段 | 说明 |
|------|------|
| 数据集名称 | 数据集标识名称 |
| 最新版本 | 版本号 |
| 数据类型 | 图像 / 文本 / 音频 |
| 标注类型 | 文本分类 / 图像分类 / 图像分割 / 物体检测 等 |
| 标注模板 | 使用的数据标注模板 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 9.2.5 创建数据集表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据集名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**数据配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 数据类型 | 下拉选择 | 是 | 图像（支持 JPG / PNG；批量上传压缩包）/ 文本（支持 TXT / JSONL）/ 音频（支持 MP3 / WAV） |
| 标注类型 | 下拉选择（联动） | 是 | 根据数据类型动态显示不同选项 |
| 标注模板 | 下拉选择 | 是 | 根据标注类型显示对应模板列表 |

**标注类型与模板联动逻辑**：

| 数据类型 | 可选标注类型 | 对应模板 |
|----------|-------------|---------|
| 图像 | 图像分类 | 图像分类模板 |
| 图像 | 图像分割 | 图像分割模板 |
| 图像 | 物体检测 | 物体检测模板 |
| 文本 | 文本分类 | 文本分类模板 |
| 文本 | 命名实体识别 | NER 模板 |
| 音频 | 语音转写 | ASR 模板 |

**数据上传**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 上传数据 | 文件上传（拖拽） | 是 | 根据数据类型限制可上传的文件格式和大小 |

---

### 9.3 机器学习标注

#### 9.3.1 功能说明
- **说明**：对机器学习数据集进行标注处理，支持分配标注人员和选择标注工具
- **URL**：`/machine-annotation`

#### 9.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 任务名称 | 标注任务名称 |
| 数据集 | 关联数据集 |
| 标注进度 | 已标注 / 总数据（如 850/1000） |
| 标注状态 | pending（待标注，蓝色）/ in_progress（标注中，蓝色）/ completed（已完成，绿色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 开始标注、详情 |

#### 9.3.3 标注状态

| 状态 | 说明 |
|------|------|
| pending | 待标注，任务已创建但未开始 |
| in_progress | 标注中，正在进行标注 |
| completed | 已完成，全部数据标注完成 |

#### 9.3.4 列表功能
- **创建标注任务**：打开创建标注任务弹窗
- **刷新**：刷新当前列表
- **开始标注**：对未开始的标注任务，点击后可进入标注页面开始标注
- **查看详情**：点击后弹出详情 Modal

#### 9.3.5 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该标注任务的完整信息：

| 字段 | 说明 |
|------|------|
| 任务名称 | 标注任务名称 |
| 数据集 | 关联数据集 |
| 标注进度 | 已标注 / 总数据（如 850/1000） |
| 标注状态 | pending（待标注，蓝色）/ in_progress（标注中，蓝色）/ completed（已完成，绿色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 开始标注 | 对未开始的标注任务，点击后可进入标注页面开始标注 |

#### 9.3.6 创建标注任务表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |
| 选择数据集 | 下拉选择（带搜索） | 是 | 显示数据集类型、标注类型、数据条数等信息 |
| 标注工具 | 下拉选择 | 是 | 内置 / 自定义 |
| 标注人员 | 下拉多选 | 否 | 从标注人员列表中选择，可多选以支持协同标注 |

> 选择数据集后，显示数据集类型、标注类型和数据条数的提示信息。

---

### 9.4 机器学习模型管理

> **说明**：机器学习模块的模型管理，与大模型训练产生的模型管理有所区别。

#### 9.4.1 功能说明
- **说明**：管理和创建用于机器学习的模型，支持部署配置和版本追溯
- **URL**：`/machine-model-management`

#### 9.4.2 列表字段

| 字段 | 说明 |
|------|------|
| 模型名称 | 模型标识名称 |
| 模型类型 | LLM / VLM 等 |
| 基础模型 | 模型的基础来源 |
| 版本数量 | 版本数量（X 个版本） |
| 状态 | 已部署（绿色）/ 未部署（灰色） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 部署、查看详情、删除 |

#### 9.4.3 列表功能
- **刷新**：刷新当前列表
- **部署**：将模型部署为在线服务
- **详情**：点击后弹出详情 Modal

#### 9.4.4 详情 Modal

点击"查看详情"按钮，弹出详情 Modal，展示该模型的完整信息：

| 字段 | 说明 |
|------|------|
| 模型名称 | 模型标识名称 |
| 模型类型 | LLM / VLM 等 |
| 基础模型 | 模型的基础来源 |
| 版本数量 | 版本数量（X 个版本） |
| 状态 | 已部署 / 未部署 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 部署 | 对未部署的模型，点击后部署模型 |

---

### 9.5 机器学习模型部署

> **说明**：机器学习模块的模型部署，将机器学习模型部署为在线服务。

#### 9.5.1 功能说明
- **说明**：将机器学习模型部署为在线服务，提供推理能力
- **URL**：`/machine-model-deployment`

#### 9.5.2 列表字段

| 字段 | 说明 |
|------|------|
| 服务名称 | 部署服务名称 |
| 模型名称 | 部署的模型名称 |
| 模型类型 | LLM / VLM 等 |
| 实例数 | 服务实例数量 |
| 状态 | running（运行中，绿色）/ stopped（已停止，灰色）/ error（异常，红色） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、停止、删除 |

#### 9.5.3 列表功能
- **部署服务**：打开创建部署弹窗
- **刷新**：刷新当前列表
- **启动**：启动已停止的服务
- **停止**：停止运行中的服务
- **详情**：点击后弹出详情 Modal

#### 9.5.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该部署服务的完整信息：

| 字段 | 说明 |
|------|------|
| 服务名称 | 部署服务名称 |
| 模型名称 | 部署的模型名称 |
| 模型类型 | LLM / VLM 等 |
| 实例数 | 服务实例数量 |
| 状态 | running / stopped / error |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| 按钮 | 说明 |
|------|------|
| 关闭 | 关闭 Modal |
| 启动 | 对已停止的服务，点击后启动服务 |
| 停止 | 对运行中的服务，点击后停止服务 |

#### 9.5.5 创建部署服务表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 服务名称 | 输入框 | 是 | 支持中英文、数字、下划线、中划线，2-64 字符 |

**资源配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| CPU请求 | 数字输入 | 是 | 单位：Core |
| 内存请求 | 数字输入 | 是 | 单位：GB |
| 显卡类型 | 下拉选择 | 是 | NVIDIA T4 / V100 / A100 |
| 实例数 | 数字输入 | 是 | 正整数 |

---

### 9.6 机器学习在线Notebook

> **说明**：机器学习模块的在线 Notebook，与模型训练的 Notebook 有所区别。

#### 9.6.1 功能说明
- **说明**：机器学习模块的交互式编程环境，支持 Python / Jupyter
- **URL**：`/machine-notebook`

#### 9.6.2 Tab 切换

页面支持两个 Tab 切换：

| Tab | 说明 |
|-----|------|
| 我的 Notebook | 用户个人创建的 Notebook 列表 |
| Notebook 广场 | 平台预置的模板案例，可复制使用 |

#### 9.6.3 列表字段

**我的 Notebook**：

| 字段 | 说明 |
|------|------|
| Notebook 名称 | Notebook 标识名称 |
| 状态 | 运行中（绿色）/ 已停止（灰色） |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 打开、详情 |

**Notebook 广场**：

| 字段 | 说明 |
|------|------|
| 案例名称 | 模板案例名称 |
| 描述 | 模板案例描述 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 复制案例、详情 |

#### 9.6.4 列表功能
- **刷新**：刷新当前列表
- **新建 Notebook**（我的 Notebook Tab）：创建新的 Notebook 环境
- **复制案例**（Notebook 广场 Tab）：从预置模板复制创建新的 Notebook
- **打开**：点击后打开 Notebook 环境
- **详情**：点击后弹出详情 Modal

#### 9.6.5 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该 Notebook 的完整信息：

| 字段 | 说明 |
|------|------|
| 名称 | Notebook 名称 |
| 状态（我的 Notebook） | 运行中 / 已停止 |
| 描述（Notebook 广场） | 模板案例描述 |
| 创建人 | 创建者 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**底部操作**：

| Tab | 按钮 | 说明 |
|-----|------|------|
| 我的 Notebook | 打开 | 关闭 Modal 并打开 Notebook 环境 |
| Notebook 广场 | 复制案例 | 关闭 Modal 并复制模板案例 |

#### 9.6.6 创建 Notebook 表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| Notebook名称 | 输入框 | 是 | 支持中英文、数字、下划线、中划线，2-64 字符 |
| 描述 | 文本域 | 否 | Notebook 描述（可选），最多 300 字符 |

---

## 十、系统管理

> 系统管理模块提供平台级配置能力，包括项目管理、集群管理、存储配置、镜像管理、基础模型管理和系统参数配置。

### 10.1 功能架构

```
系统管理
├── 项目管理
├── 权限配置
├── 集群管理
├── 存储配置
├── 镜像管理
├── 基础模型管理
└── 系统配置
```

---

### 10.2 项目管理

#### 10.2.1 功能说明
- **说明**：管理平台项目，支持项目创建、成员管理和 SSH 配置
- **URL**：`/admin/projects`

#### 10.2.2 列表字段

| 字段 | 说明 |
|------|------|
| 项目名称 | 项目标识名称 |
| 项目描述 | 项目描述（超长时省略） |
| 绑定集群 | 关联的 Kubernetes 集群名称 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 编辑、SSH 配置、成员管理 |

#### 10.2.3 列表功能
- **新建项目**：打开创建项目弹窗
- **刷新**：刷新当前列表
- **详情**：点击后弹出详情 Modal，包含基本信息、成员管理和 SSH 配置标签页

#### 10.2.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，采用 Tab 页结构组织内容：

**基本信息 Tab**：

| 字段 | 说明 |
|------|------|
| 项目名称 | 项目标识名称 |
| 项目描述 | 项目描述（超长时省略） |
| 绑定集群 | 关联的 Kubernetes 集群名称 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**成员管理 Tab**：

| 字段 | 说明 |
|------|------|
| 成员账号 | 项目成员账号 |
| 成员角色 | 该用户在外部控制台已授权的角色，添加成员时选择 |
| 加入时间 | 加入时间 |
| 操作 | 移除 |

- **添加成员**：点击后打开添加成员弹窗；项目创建/编辑不再维护“项目管理员”字段，项目权限通过成员管理分配

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 用户账号 | 下拉选择/搜索选择 | 是 | 从外部控制台同步的用户中选择 |
| 成员角色 | 下拉选择 | 是 | 仅展示该用户在外部控制台已授权的角色 |
| 备注 | 文本域 | 否 | 备注信息 |

**SSH配置 Tab**：

| 字段 | 说明 |
|------|------|
| SSH Key 名称 | SSH Key 标识名称 |
| SSH Key | SSH 公钥内容（代码样式） |
| 创建时间 | 创建时间 |
| 操作 | 删除 |

- **添加SSH密钥**：点击后打开添加 SSH 密钥弹窗

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| SSH Key 名称 | 输入框 | 是 | 2-64 字符 |
| SSH Key | 文本域 | 是 | SSH 公钥内容 |

#### 10.2.5 创建项目表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 项目名称 | 输入框 | 是 | 2-64 字符 |
| 项目描述 | 文本域 | 否 | 最多 300 字符 |
| 绑定集群 | 下拉选择 | 是 | 从可用集群列表中选择，显示节点数信息 |

---

### 10.3 集群管理

#### 10.3.1 功能说明
- **说明**：Kubernetes 集群管理，支持集群导入、连接测试、存储和仓库绑定
- **URL**：`/admin/kubernetes`

#### 10.3.2 列表字段

| 字段 | 说明 |
|------|------|
| 集群名称 | K8s 集群标识 |
| API Server | API 服务端点地址（代码样式） |
| 标签 | 集群标签（内联标签组） |
| 节点数 | 集群节点数量 |
| 连接状态 | 已连接（绿色）/ 未连接（红色） |
| 挂载状态 | 已挂载（绿色）/ 未挂载（红色） |
| 存储配置 | 已配置 / 未配置 |
| 镜像仓库 | 已配置 / 未配置 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 测试连接、绑定存储配置、绑定仓库配置、编辑、删除 |

#### 10.3.3 列表功能
- **导入集群**：打开导入集群弹窗
- **刷新**：刷新当前列表
- **测试连接**：点击后检测集群连通性，显示状态反馈
- **绑定存储配置**：点击后绑定存储配置
- **绑定仓库配置**：点击后绑定仓库配置
- **详情**：点击后弹出详情 Modal

#### 10.3.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该集群的完整信息：

| 字段 | 说明 |
|------|------|
| 集群名称 | K8s 集群标识 |
| API Server | API 服务端点地址（代码样式） |
| 标签 | 集群标签（内联标签组） |
| 节点数 | 集群节点数量 |
| 连接状态 | 已连接（绿色）/ 未连接（红色） |
| 挂载状态 | 已挂载（绿色）/ 未挂载（红色） |
| 存储配置 | 已配置 / 未配置 |
| 镜像仓库 | 已配置 / 未配置 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 10.3.5 导入集群表单

**集群信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 集群名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**连接配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| Kubeconfig 文件 | 文件上传按钮 | 是 | 上传 .kubeconfig 文件 |
| API Server 地址 | 输入框（URL 校验） | 是 | K8s API 端点，自动从 Kubeconfig 填充 |
| 测试连接 | 按钮 | 否 | 点击后检测集群连通性，显示连接状态反馈 |

**连接测试状态流转**：
```
idle → testing（加载中，显示"测试连接中..."）→ success（显示绿色"连接成功"提示）/ failed（显示红色"连接失败"提示）
```

**标签配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 集群标签 | 下拉多选标签 | 否 | 从预置标签列表中选择（GPU / CPU / 训练 / 推理 / 开发 / 生产 / 测试） |

---

### 10.4 存储配置

#### 10.4.1 功能说明
- **说明**：存储配置管理，支持不同类型的存储，并进行文件系统格式化
- **URL**：`/admin/storage`

#### 10.4.2 列表字段

| 字段 | 说明 |
|------|------|
| 存储名称 | 存储标识名称 |
| 描述 | 存储描述（超长时省略） |
| 存储类型 | TOS（火山引擎 TOS）/ OSS（阿里云 OSS）/ S3（AWS S3 兼容）/ NFS / Ceph |
| 集群数量 | 关联集群数量 |
| 连接状态 | 连接正常（绿色）/ 未连接（红色） |
| 最后测试时间 | 最后一次连接测试时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 测试连接、查看详情、文件系统格式化（危险操作，需二次确认） |

#### 10.4.3 列表功能
- **新建配置**：打开创建存储配置弹窗
- **搜索/重置**：按存储名称搜索
- **刷新**：刷新当前列表
- **测试连接**：点击后验证存储连通性，显示状态反馈
- **详情**：点击后弹出详情 Modal

#### 10.4.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该存储配置的完整信息：

| 字段 | 说明 |
|------|------|
| 存储名称 | 存储标识名称 |
| 描述 | 存储描述（超长时省略） |
| 存储类型 | TOS / OSS / S3 / NFS / Ceph |
| 集群数量 | 关联集群数量 |
| 连接状态 | 连接正常（绿色）/ 未连接（红色） |
| 最后测试时间 | 最后一次连接测试时间 |

#### 10.4.5 创建存储配置表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 存储名称 | 输入框 | 是 | 2-64 字符 |
| 描述 | 文本域 | 否 | 最多 300 字符 |

**连接配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 存储类型 | 下拉选择 | 是 | TOS（火山引擎 TOS）/ OSS（阿里云 OSS）/ S3（AWS S3 兼容）/ NFS / Ceph |
| 访问密钥 ID | 输入框 | 是 | 对象存储访问密钥 |
| 访问密钥密钥 | 密码输入框 | 是 | 对象存储访问密钥密码 |
| 存储桶/卷名 | 输入框 | 是 | 对象存储桶名或 NFS/Ceph 卷名 |
| 访问域名/地址 | 输入框（URL 校验） | 是 | 存储服务访问端点 |
| 测试连接 | 按钮 | 否 | 点击后验证存储连通性，显示状态反馈 |

---

### 10.5 镜像管理

#### 10.5.1 功能说明
- **说明**：镜像仓库配置管理，管理镜像仓库的接入配置
- **URL**：`/admin/registry`

#### 10.5.2 列表字段

| 字段 | 说明 |
|------|------|
| 仓库名称 | 仓库标识名称 |
| 命名空间 | 仓库命名空间 |
| 仓库地址 | 仓库 URL 地址（代码样式） |
| 认证方式 | 用户名密码 / Token / 公开 |
| 绑定集群 | 已绑定集群数量（X 个） |
| 状态 | 正常（绿色）/ 异常（红色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 测试连接、详情、删除 |

#### 10.5.3 列表功能
- **新建配置**：打开创建镜像仓库弹窗
- **搜索**：按仓库名称搜索
- **刷新**：刷新当前列表
- **测试连接**：点击后验证镜像仓库连通性
- **详情**：点击后弹出详情 Modal，包含镜像列表

#### 10.5.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该镜像仓库的完整信息和关联的镜像列表：

**基本信息区域**：

| 字段 | 说明 |
|------|------|
| 仓库名称 | 仓库标识名称 |
| 命名空间 | 仓库命名空间 |
| 仓库地址 | 仓库 URL 地址（代码样式） |
| 认证方式 | 用户名密码 / Token / 公开 |
| 绑定集群 | 已绑定集群数量（X 个） |
| 状态 | 正常（绿色）/ 异常（红色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

**镜像列表区域**：

| 字段 | 说明 |
|------|------|
| 镜像名称 | 镜像标识名称 |
| 镜像标签 | 镜像版本标签 |
| 镜像大小 | 镜像大小 |
| 更新时间 | 最后更新时间 |

#### 10.5.5 创建镜像仓库表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 仓库名称 | 输入框 | 是 | 2-64 字符 |
| 仓库地址 | 输入框（URL 校验） | 是 | 镜像仓库 URL 地址 |
| 命名空间 | 输入框 | 是 | 仓库命名空间 |

**认证配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 认证方式 | 下拉选择 | 是 | 用户名密码 / Token / 公开 |

**认证字段联动**：

| 认证方式 | 显示字段 |
|----------|---------|
| 用户名密码 | 用户名 + 密码 |
| Token | Token |
| 公开 | 无额外字段 |

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 用户名 | 输入框 | 是（用户名密码模式） | 仓库认证用户名 |
| 密码 | 密码输入框 | 是（用户名密码模式） | 仓库认证密码 |
| Token | 密码输入框 | 是（Token 模式） | 仓库认证 Token |

**测试连接**：点击后验证镜像仓库连通性。

---

### 10.6 基础模型管理

#### 10.6.1 功能说明
- **说明**：基础模型管理，支持模型启动、编辑、删除和日志查看
- **URL**：`/admin/base-model`

#### 10.6.2 列表字段

| 字段 | 说明 |
|------|------|
| 模型 Code | 模型唯一标识（代码样式） |
| 模型名称 | 模型显示名称 |
| 描述 | 模型说明（超长时省略） |
| 模型类型 | LLM（大语言模型）/ VLM（视觉语言模型）/ ASR（语音识别）/ Embedding（向量嵌入），紫色标签 |
| 提供商 | 模型提供方名称 |
| 支持能力 | 多标签展示（训练 / 推理 / 对话 / 代码 / 图像理解 / 多模态 / 推理能力 / 文本生成） |
| 状态 | 已完成（绿色）/ 已终止（灰色）/ 运行中（蓝色） |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |
| 操作 | 启动、编辑、日志、终止、删除 |

#### 10.6.3 列表功能
- **新增模型**：打开创建基础模型弹窗
- **搜索/重置**：按模型 Code 搜索
- **刷新**：刷新当前列表
- **启动**：启动模型服务（非运行状态可用）
- **终止**：终止运行中的模型服务
- **详情**：点击后弹出详情 Modal

#### 10.6.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该基础模型的完整信息：

| 字段 | 说明 |
|------|------|
| 模型 Code | 模型唯一标识（代码样式） |
| 模型名称 | 模型显示名称 |
| 描述 | 模型说明（超长时省略） |
| 模型类型 | LLM / VLM / ASR / Embedding |
| 提供商 | 模型提供方名称 |
| 支持能力 | 多标签展示 |
| 状态 | 已完成 / 已终止 / 运行中 |
| 创建时间 | 创建时间（YYYY/MM/DD HH:mm:ss） |

#### 10.6.5 创建基础模型表单

**基本信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 模型 Code | 输入框 | 是 | 仅支持小写字母和数字，自动转为小写 |
| 模型名称 | 输入框 | 是 | 模型显示名称，2-64 字符 |

**模型配置**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 模型类型 | 下拉选择 | 是 | LLM（大语言模型）/ VLM（视觉语言模型）/ ASR（语音识别）/ Embedding（向量嵌入） |
| 模型提供商 | 下拉选择 | 是 | 模型提供方名称 |
| 模型地址 | 输入框 | 是 | 模型文件存储地址或 HuggingFace 模型路径 |

**支持能力**（多选）：

| 能力 | 说明 |
|------|------|
| 文本生成 | 支持文本生成任务 |
| 对话 | 支持对话任务 |
| 代码 | 支持代码相关任务 |
| 图像理解 | 支持图像理解任务 |
| 多模态 | 支持多模态任务 |
| 推理能力 | 支持推理任务 |
| 训练 | 支持模型训练 |
| 推理 | 支持模型推理 |

**其他信息**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 描述 | 文本域 | 否 | 模型详细描述，最多 300 字符 |

---

### 10.7 系统配置

#### 10.7.1 功能说明
- **说明**：系统属性配置管理，配置平台的各项系统参数，支持属性配置、标签配置、数据管理和模型服务四个子模块
- **URL**：`/admin/settings`

#### 10.7.2 属性配置列表字段

| 字段 | 说明 |
|------|------|
| 属性名称 | 配置项名称 |
| 属性描述 | 配置说明（超长时省略） |
| 输入方式 | 文本输入 / 数字输入 / 开关 / 下拉选择，紫色徽标 |
| 属性值 | 当前配置值（代码样式） |
| 属性分组 | 所属配置分组（紫色徽标），如：训练配置 / 存储配置 / 安全配置 / 会话配置 / 业务测试1 / 业务测试2 |
| 是否必填 | 是（红色）/ 否（灰色） |
| 操作 | 编辑、删除 |

#### 10.7.3 列表功能
- **添加属性**：打开添加属性弹窗
- **搜索**：按属性名称搜索
- **刷新**：刷新当前列表
- **详情**：点击后弹出详情 Modal

#### 10.7.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该系统属性的完整信息：

| 字段 | 说明 |
|------|------|
| 属性名称 | 配置项名称 |
| 属性描述 | 配置说明（超长时省略） |
| 输入方式 | 文本输入 / 数字输入 / 开关 / 下拉选择 |
| 属性值 | 当前配置值（代码样式） |
| 属性分组 | 所属配置分组，如：训练配置 / 存储配置 / 安全配置 |
| 是否必填 | 是 / 否 |

#### 10.7.5 添加属性表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 属性名称 | 输入框 | 是 | 2-64 字符 |
| 属性描述 | 文本域 | 否 | 属性说明 |
| 输入方式 | 下拉选择 | 是 | 文本输入 / 数字输入 / 开关 / 下拉选择 |
| 属性分组 | 下拉选择 | 是 | 训练配置 / 存储配置 / 安全配置 / 会话配置 / 其他 |
| 属性值 | 输入框 | 是 | 当前配置的默认值 |
| 是否必填 | 开关 | 否 | 开启后该属性为必填项 |

---

### 10.8 权限配置（V1.14）

#### 10.8.1 需求背景

Lab 需要基于外部控制台同步的用户、角色和授权关系，在平台内配置角色对应的操作权限和可操作资源范围，避免用户拥有按钮入口后越权操作他人创建的数据、任务、模型或服务。

#### 10.8.2 目标范围

- Lab 只读取外部控制台同步的用户、角色和用户授权关系，不提供用户角色授权、角色创建、角色删除、角色改名能力。
- 权限配置拆分为“操作权限”和“可操作资源范围”两个 Tab，二者共同参与业务操作拦截。
- 内置角色固定为平台管理员、项目管理员、训练工程师；内置角色只读，不允许编辑操作权限和可操作资源范围。
- 外部控制台同步的新角色允许在 Lab 内编辑操作权限和可操作资源范围。
- 项目创建/编辑不再维护项目管理员字段；项目成员通过成员管理添加，并选择该用户在外部控制台已授权的项目角色。

#### 10.8.3 页面结构

- 顶部仅展示权限配置功能描述：管理角色在 Lab 内的功能授权与资源归属控制。
- 左侧展示角色搜索和角色列表，角色卡片展示角色名称与全部资源摘要，不展示“内置”“外部角色”等额外标识。
- 右侧包含操作权限 Tab 和可操作资源范围 Tab。
- 操作权限 Tab 使用权限树展示菜单、页面和业务动作权限。
- 可操作资源范围 Tab 按大模型、机器学习、系统管理三部分展示个人资源和全部资源。

#### 10.8.4 操作权限规则

- 操作权限覆盖菜单入口、页面查看、创建、编辑、删除、启动、停止、提交、下载、导出、引用等业务动作。
- 内置角色只能查看权限配置，不允许修改。
- 外部同步角色可勾选或取消操作权限，保存后影响该角色在 Lab 内的功能入口与按钮能力。
- 用户执行业务动作时，必须同时满足项目成员身份、当前项目角色、菜单权限和操作权限。

#### 10.8.5 可操作资源范围规则

- 可操作资源范围只回答“已授权操作可对谁创建的资源生效”，不替代操作权限。
- 大模型、机器学习、系统管理均包含“个人资源”和“全部资源”两种范围。
- “个人资源”默认勾选且不可取消；拥有对应操作权限时，可对本人创建的数据、任务、模型、服务等资源执行该操作。
- “全部资源”勾选后；拥有对应操作权限时，可对对应业务域内全部用户创建的数据、任务、模型、服务等资源执行该操作。
- 平台管理员和项目管理员默认拥有大模型、机器学习的全部资源；平台管理员同时默认拥有系统管理全部资源。
- 下载、导出、引用、查看详情等均属于操作权限；是否可对他人资源执行，由可操作资源范围决定。
- 系统管理的可操作资源范围仅控制存在创建人归属的系统资源。
- 判断个人资源时，以资源创建人账号与当前登录账号是否一致为准；历史 mock 数据需要补齐或映射创建人账号。

#### 10.8.6 权限不足规则

- 没有“全部资源”时，用户仍可在列表看到并搜索到他人资源的基础行信息。
- 进入详情、编辑、删除、启动、停止、下载、提交等操作时，如资源创建人不是当前账号，则提示“权限不足”。
- 权限不足时不进入详情、不提交表单、不改变资源或任务状态。

#### 10.8.7 项目管理联动

- 项目创建/编辑保留项目名称、描述、绑定集群等字段，删除项目管理员字段。
- 成员管理添加成员时先选择用户，再选择该用户在外部控制台已授权的项目角色。
- 成员列表展示成员账号、用户名、邮箱、项目角色、加入时间和操作。
- 成员进入项目后，已授权操作可对个人资源还是全部资源生效，由该项目角色的可操作资源范围决定。

#### 10.8.8 验收标准

- 权限配置页不出现用户角色授权、新建角色、删除角色、编辑角色名称入口。
- 页面展示三个内置角色和多个外部同步角色；内置角色只读，外部角色可编辑。
- 可操作资源范围 Tab 三个业务域均展示个人资源和全部资源；个人资源不可取消。
- 无全部资源用户可以搜索到他人资源，但详情、编辑、删除、启动、下载、导出、引用等操作统一提示权限不足。
- 项目成员添加时必须选择该用户已授权角色，成员列表展示项目角色字段。

---

## 十一、平台管理员

### 11.1 功能说明
- **说明**：平台管理员成员管理，添加或移除平台管理员
- **URL**：`/admin/platform-management`

#### 11.1.2 列表字段

| 字段 | 说明 |
|------|------|
| 账号 | 管理员账号（蓝色代码样式） |
| 用户名 | 管理员用户名 |
| 邮箱 | 管理员邮箱（脱敏显示，如 1\*\*\*\*@qq.com） |
| 加入时间 | 加入时间（YYYY-MM-DD HH:mm:ss） |
| 操作 | 删除（危险操作） |

#### 11.1.3 列表功能
- **添加成员**：打开添加成员弹窗
- **刷新**：刷新当前列表
- **详情**：点击后弹出详情 Modal

#### 11.1.4 详情 Modal

点击"详情"按钮，弹出详情 Modal，展示该管理员的完整信息：

| 字段 | 说明 |
|------|------|
| 账号 | 管理员账号（蓝色代码样式） |
| 用户名 | 管理员用户名 |
| 邮箱 | 管理员邮箱（脱敏显示） |
| 加入时间 | 加入时间（YYYY-MM-DD HH:mm:ss） |

#### 11.1.5 添加成员表单

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 用户账号 | 输入框 | 是 | 支持字母和数字组合 |
| 备注 | 文本域 | 否 | 管理员备注信息 |

---

## 十二、通用交互规范

### 12.1 定时任务配置

所有支持定时配置的任务（训练、推理、标注、清洗、评估），均采用统一的交互模式：

**交互设计**：
- 使用开关（Switch）控制是否启用定时，默认关闭
- 开关关闭时，不渲染时间选择器，表单数据中不含时间字段
- 开关打开后，右侧展开 DatePicker 时间选择器
- 时间格式固定为 `YYYY-MM-DD HH:mm`（24 小时制）
- 定时时间必须晚于当前时间，否则给出校验提示

**数据存储**：
- `scheduleEnabled`：布尔值，控制是否启用定时
- `schedule`（或 `taskSchedule`）：Date 类型，存储选中的时间点

**定时任务执行流程**：
1. 用户开启定时开关，选择时间
2. 任务创建后进入"定时待启动"状态
3. 系统在指定时间自动触发任务启动
4. 任务后续流转与非定时任务一致

### 12.2 表单提交校验

- 所有必填字段未填写时，点击提交按钮触发表单校验，阻止提交
- 页面顶部显示校验提示："请完善必填项后再提交"
- 校验失败时表单自动滚动到第一个错误字段

### 12.3 列表通用功能

- **搜索**：支持按名称字段搜索，输入关键词即时过滤
- **刷新**：重新请求列表数据
- **分页**：列表支持分页展示（每页 10 条）
- **空状态**：无数据时显示空状态提示和插图
- **加载状态**：列表加载中显示骨架屏或 Loading 状态

### 12.4 删除操作

- 所有删除操作均为危险操作（红色按钮或危险样式）
- 删除前弹出确认对话框，提示操作后果
- 删除成功后显示成功消息

### 12.5 任务状态颜色规范

| 状态类型 | 颜色 |
|----------|------|
| 待处理 / 待启动 / pending | 蓝色 / #1677ff |
| 进行中 / 运行中 / 标注中 | 蓝色 / #1677ff |
| 已完成 / 成功 / completed | 绿色 / #52c41a |
| 失败 / 错误 / 异常 | 红色 / #ff4d4f |
| 已停止 / 已终止 | 灰色 / #999999 |
| 已创建 / 定时待启动 | 浅蓝色 / #1677ff |

---

## 十三、术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| SFT | Supervised Fine-Tuning | 监督微调，在预训练模型基础上使用标注数据进行的微调 |
| DPO | Direct Preference Optimization | 直接偏好优化，强化学习方法之一，无需单独训练奖励模型 |
| RM | Reward Model | 奖励模型，用于评估生成质量，是 PPO 强化学习的核心组件 |
| PPO | Proximal Policy Optimization | 近端策略优化，强化学习方法，使用奖励模型优化语言模型策略 |
| RLHF | Reinforcement Learning from Human Feedback | 基于人类反馈的强化学习，SFT→RM→PPO 的完整链路 |
| LoRA | Low-Rank Adaptation | 低秩适配，高效微调方法，仅更新少量参数 |
| VLM | Vision Language Model | 视觉语言模型，支持图像+文本多模态理解与生成 |
| ASR | Automatic Speech Recognition | 自动语音识别，将语音转换为文本 |
| Embedding | Text Embedding | 文本向量嵌入，将文本映射为稠密向量 |
| bf16 | Brain Float 16 | 一种混合精度训练格式，兼顾训练速度与数值稳定性 |
| RoPE | Rotary Position Embedding | 旋转位置编码，大模型常用位置编码方案 |
| YARN | Yet Another RoPE extensioN | 一种 RoPE 上下文窗口扩展方法 |
| Cutoff Length | - | 训练样本的最大 token 长度限制 |
| Warmup Ratio | - | 学习率预热比例 |
| Gradient Accumulation | - | 梯度累积，通过累积多个小批次的梯度模拟大批次训练 |
| Gradient Checkpointing | - | 梯度检查点，通过重计算减少显存占用的技术 |
| MinHash | - | 一种近似去重算法，适合大规模数据去重 |
| SimHash | - | 一种局部敏感哈希算法，适合相似文档去重 |
| Kubernetes | K8s | 容器编排平台，用于管理集群资源 |
| NFS | Network File System | 网络文件系统，一种分布式存储协议 |
| TOS | Volcano Engine TOS | 火山引擎对象存储 |
| OSS | Alibaba Cloud OSS | 阿里云对象存储 |
| S3 | Amazon S3 Compatible | AWS S3 兼容的对象存储 |
| Ceph | - | 分布式存储系统 |
| vLLM | - | 高性能 LLMs 推理引擎，支持 PagedAttention |
| DGI Server | - | DeepGene Inference Server，深度学习推理服务引擎 |
| API Server | - | Kubernetes API 服务端点 |
| QPS | Queries Per Second | 每秒查询数，衡量服务吞吐能力 |

---
